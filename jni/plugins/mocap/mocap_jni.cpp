// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/mocap/mocap.proto)

#include <jni.h>
#include "cmavsdk/plugins/mocap/mocap.h"
#include "../../jni_utils.h"

#include <cstdint>
#include <memory>
#include <string>
#include <utility>
#include <vector>

using namespace mavsdk::jni;

namespace {


struct PositionBodyFromJava;
struct PositionBodyArrayFromJava;
struct AngleBodyFromJava;
struct AngleBodyArrayFromJava;
struct SpeedBodyFromJava;
struct SpeedBodyArrayFromJava;
struct SpeedNedFromJava;
struct SpeedNedArrayFromJava;
struct AngularVelocityBodyFromJava;
struct AngularVelocityBodyArrayFromJava;
struct CovarianceFromJava;
struct CovarianceArrayFromJava;
struct QuaternionFromJava;
struct QuaternionArrayFromJava;
struct VisionPositionEstimateFromJava;
struct VisionPositionEstimateArrayFromJava;
struct VisionSpeedEstimateFromJava;
struct VisionSpeedEstimateArrayFromJava;
struct AttitudePositionMocapFromJava;
struct AttitudePositionMocapArrayFromJava;
struct OdometryFromJava;
struct OdometryArrayFromJava;

struct PositionBodyFromJava {
    mavsdk_mocap_position_body_t value{};

    PositionBodyFromJava(JNIEnv* env, jobject object);
    ~PositionBodyFromJava();
};

struct PositionBodyArrayFromJava {
    std::vector<std::unique_ptr<PositionBodyFromJava>> holders;
    std::vector<mavsdk_mocap_position_body_t> values;

    PositionBodyArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<PositionBodyFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct AngleBodyFromJava {
    mavsdk_mocap_angle_body_t value{};

    AngleBodyFromJava(JNIEnv* env, jobject object);
    ~AngleBodyFromJava();
};

struct AngleBodyArrayFromJava {
    std::vector<std::unique_ptr<AngleBodyFromJava>> holders;
    std::vector<mavsdk_mocap_angle_body_t> values;

    AngleBodyArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<AngleBodyFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct SpeedBodyFromJava {
    mavsdk_mocap_speed_body_t value{};

    SpeedBodyFromJava(JNIEnv* env, jobject object);
    ~SpeedBodyFromJava();
};

struct SpeedBodyArrayFromJava {
    std::vector<std::unique_ptr<SpeedBodyFromJava>> holders;
    std::vector<mavsdk_mocap_speed_body_t> values;

    SpeedBodyArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<SpeedBodyFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct SpeedNedFromJava {
    mavsdk_mocap_speed_ned_t value{};

    SpeedNedFromJava(JNIEnv* env, jobject object);
    ~SpeedNedFromJava();
};

struct SpeedNedArrayFromJava {
    std::vector<std::unique_ptr<SpeedNedFromJava>> holders;
    std::vector<mavsdk_mocap_speed_ned_t> values;

    SpeedNedArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<SpeedNedFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct AngularVelocityBodyFromJava {
    mavsdk_mocap_angular_velocity_body_t value{};

    AngularVelocityBodyFromJava(JNIEnv* env, jobject object);
    ~AngularVelocityBodyFromJava();
};

struct AngularVelocityBodyArrayFromJava {
    std::vector<std::unique_ptr<AngularVelocityBodyFromJava>> holders;
    std::vector<mavsdk_mocap_angular_velocity_body_t> values;

    AngularVelocityBodyArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<AngularVelocityBodyFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct CovarianceFromJava {
    mavsdk_mocap_covariance_t value{};
    std::vector<float> covariance_matrixValues;

    CovarianceFromJava(JNIEnv* env, jobject object);
    ~CovarianceFromJava();
};

struct CovarianceArrayFromJava {
    std::vector<std::unique_ptr<CovarianceFromJava>> holders;
    std::vector<mavsdk_mocap_covariance_t> values;

    CovarianceArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<CovarianceFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct QuaternionFromJava {
    mavsdk_mocap_quaternion_t value{};

    QuaternionFromJava(JNIEnv* env, jobject object);
    ~QuaternionFromJava();
};

struct QuaternionArrayFromJava {
    std::vector<std::unique_ptr<QuaternionFromJava>> holders;
    std::vector<mavsdk_mocap_quaternion_t> values;

    QuaternionArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<QuaternionFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct VisionPositionEstimateFromJava {
    mavsdk_mocap_vision_position_estimate_t value{};
    std::unique_ptr<PositionBodyFromJava> position_bodyValue;
    std::unique_ptr<AngleBodyFromJava> angle_bodyValue;
    std::unique_ptr<CovarianceFromJava> pose_covarianceValue;

    VisionPositionEstimateFromJava(JNIEnv* env, jobject object);
    ~VisionPositionEstimateFromJava();
};

struct VisionPositionEstimateArrayFromJava {
    std::vector<std::unique_ptr<VisionPositionEstimateFromJava>> holders;
    std::vector<mavsdk_mocap_vision_position_estimate_t> values;

    VisionPositionEstimateArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<VisionPositionEstimateFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct VisionSpeedEstimateFromJava {
    mavsdk_mocap_vision_speed_estimate_t value{};
    std::unique_ptr<SpeedNedFromJava> speed_nedValue;
    std::unique_ptr<CovarianceFromJava> speed_covarianceValue;

    VisionSpeedEstimateFromJava(JNIEnv* env, jobject object);
    ~VisionSpeedEstimateFromJava();
};

struct VisionSpeedEstimateArrayFromJava {
    std::vector<std::unique_ptr<VisionSpeedEstimateFromJava>> holders;
    std::vector<mavsdk_mocap_vision_speed_estimate_t> values;

    VisionSpeedEstimateArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<VisionSpeedEstimateFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct AttitudePositionMocapFromJava {
    mavsdk_mocap_attitude_position_mocap_t value{};
    std::unique_ptr<QuaternionFromJava> qValue;
    std::unique_ptr<PositionBodyFromJava> position_bodyValue;
    std::unique_ptr<CovarianceFromJava> pose_covarianceValue;

    AttitudePositionMocapFromJava(JNIEnv* env, jobject object);
    ~AttitudePositionMocapFromJava();
};

struct AttitudePositionMocapArrayFromJava {
    std::vector<std::unique_ptr<AttitudePositionMocapFromJava>> holders;
    std::vector<mavsdk_mocap_attitude_position_mocap_t> values;

    AttitudePositionMocapArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<AttitudePositionMocapFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};
struct OdometryFromJava {
    mavsdk_mocap_odometry_t value{};
    std::unique_ptr<PositionBodyFromJava> position_bodyValue;
    std::unique_ptr<QuaternionFromJava> qValue;
    std::unique_ptr<SpeedBodyFromJava> speed_bodyValue;
    std::unique_ptr<AngularVelocityBodyFromJava> angular_velocity_bodyValue;
    std::unique_ptr<CovarianceFromJava> pose_covarianceValue;
    std::unique_ptr<CovarianceFromJava> velocity_covarianceValue;

    OdometryFromJava(JNIEnv* env, jobject object);
    ~OdometryFromJava();
};

struct OdometryArrayFromJava {
    std::vector<std::unique_ptr<OdometryFromJava>> holders;
    std::vector<mavsdk_mocap_odometry_t> values;

    OdometryArrayFromJava(JNIEnv* env, jobjectArray array) {
        const jsize count = array ? env->GetArrayLength(array) : 0;
        holders.reserve(static_cast<size_t>(count));
        values.reserve(static_cast<size_t>(count));
        for (jsize i = 0; i < count; ++i) {
            jobject element = env->GetObjectArrayElement(array, i);
            auto holder = std::make_unique<OdometryFromJava>(env, element);
            values.push_back(holder->value);
            holders.push_back(std::move(holder));
            env->DeleteLocalRef(element);
        }
    }
};

PositionBodyFromJava::PositionBodyFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID x_mField = env->GetFieldID(
        clazz, "xM", "F");
    value.x_m =
        static_cast<float>(env->GetFloatField(object, x_mField));
    jfieldID y_mField = env->GetFieldID(
        clazz, "yM", "F");
    value.y_m =
        static_cast<float>(env->GetFloatField(object, y_mField));
    jfieldID z_mField = env->GetFieldID(
        clazz, "zM", "F");
    value.z_m =
        static_cast<float>(env->GetFloatField(object, z_mField));
    env->DeleteLocalRef(clazz);
}

PositionBodyFromJava::~PositionBodyFromJava() = default;
AngleBodyFromJava::AngleBodyFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID roll_radField = env->GetFieldID(
        clazz, "rollRad", "F");
    value.roll_rad =
        static_cast<float>(env->GetFloatField(object, roll_radField));
    jfieldID pitch_radField = env->GetFieldID(
        clazz, "pitchRad", "F");
    value.pitch_rad =
        static_cast<float>(env->GetFloatField(object, pitch_radField));
    jfieldID yaw_radField = env->GetFieldID(
        clazz, "yawRad", "F");
    value.yaw_rad =
        static_cast<float>(env->GetFloatField(object, yaw_radField));
    env->DeleteLocalRef(clazz);
}

AngleBodyFromJava::~AngleBodyFromJava() = default;
SpeedBodyFromJava::SpeedBodyFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID x_m_sField = env->GetFieldID(
        clazz, "xMS", "F");
    value.x_m_s =
        static_cast<float>(env->GetFloatField(object, x_m_sField));
    jfieldID y_m_sField = env->GetFieldID(
        clazz, "yMS", "F");
    value.y_m_s =
        static_cast<float>(env->GetFloatField(object, y_m_sField));
    jfieldID z_m_sField = env->GetFieldID(
        clazz, "zMS", "F");
    value.z_m_s =
        static_cast<float>(env->GetFloatField(object, z_m_sField));
    env->DeleteLocalRef(clazz);
}

SpeedBodyFromJava::~SpeedBodyFromJava() = default;
SpeedNedFromJava::SpeedNedFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID north_m_sField = env->GetFieldID(
        clazz, "northMS", "F");
    value.north_m_s =
        static_cast<float>(env->GetFloatField(object, north_m_sField));
    jfieldID east_m_sField = env->GetFieldID(
        clazz, "eastMS", "F");
    value.east_m_s =
        static_cast<float>(env->GetFloatField(object, east_m_sField));
    jfieldID down_m_sField = env->GetFieldID(
        clazz, "downMS", "F");
    value.down_m_s =
        static_cast<float>(env->GetFloatField(object, down_m_sField));
    env->DeleteLocalRef(clazz);
}

SpeedNedFromJava::~SpeedNedFromJava() = default;
AngularVelocityBodyFromJava::AngularVelocityBodyFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID roll_rad_sField = env->GetFieldID(
        clazz, "rollRadS", "F");
    value.roll_rad_s =
        static_cast<float>(env->GetFloatField(object, roll_rad_sField));
    jfieldID pitch_rad_sField = env->GetFieldID(
        clazz, "pitchRadS", "F");
    value.pitch_rad_s =
        static_cast<float>(env->GetFloatField(object, pitch_rad_sField));
    jfieldID yaw_rad_sField = env->GetFieldID(
        clazz, "yawRadS", "F");
    value.yaw_rad_s =
        static_cast<float>(env->GetFloatField(object, yaw_rad_sField));
    env->DeleteLocalRef(clazz);
}

AngularVelocityBodyFromJava::~AngularVelocityBodyFromJava() = default;
CovarianceFromJava::CovarianceFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID covariance_matrixField = env->GetFieldID(
        clazz, "covarianceMatrix", "[F");
    auto covariance_matrixArray =
        static_cast<jfloatArray>(env->GetObjectField(object, covariance_matrixField));
    const jsize covariance_matrixCount =
        covariance_matrixArray ? env->GetArrayLength(covariance_matrixArray) : 0;
    std::vector<jfloat> covariance_matrixJavaValues(
        static_cast<size_t>(covariance_matrixCount));
    if (covariance_matrixCount > 0) {
        env->GetFloatArrayRegion(
            covariance_matrixArray, 0, covariance_matrixCount,
            covariance_matrixJavaValues.data());
        covariance_matrixValues.reserve(static_cast<size_t>(covariance_matrixCount));
        for (auto item : covariance_matrixJavaValues) {
            covariance_matrixValues.push_back(static_cast<float>(item));
        }
    }
    value.covariance_matrix = covariance_matrixValues.data();
    value.covariance_matrix_size =
        static_cast<size_t>(covariance_matrixCount);
    env->DeleteLocalRef(covariance_matrixArray);
    env->DeleteLocalRef(clazz);
}

CovarianceFromJava::~CovarianceFromJava() = default;
QuaternionFromJava::QuaternionFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID wField = env->GetFieldID(
        clazz, "w", "F");
    value.w =
        static_cast<float>(env->GetFloatField(object, wField));
    jfieldID xField = env->GetFieldID(
        clazz, "x", "F");
    value.x =
        static_cast<float>(env->GetFloatField(object, xField));
    jfieldID yField = env->GetFieldID(
        clazz, "y", "F");
    value.y =
        static_cast<float>(env->GetFloatField(object, yField));
    jfieldID zField = env->GetFieldID(
        clazz, "z", "F");
    value.z =
        static_cast<float>(env->GetFloatField(object, zField));
    env->DeleteLocalRef(clazz);
}

QuaternionFromJava::~QuaternionFromJava() = default;
VisionPositionEstimateFromJava::VisionPositionEstimateFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID time_usecField = env->GetFieldID(
        clazz, "timeUsec", "J");
    value.time_usec =
        static_cast<uint64_t>(env->GetLongField(object, time_usecField));
    jfieldID position_bodyField = env->GetFieldID(
        clazz, "positionBody", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody;");
    jobject position_bodyObject =
        env->GetObjectField(object, position_bodyField);
    position_bodyValue =
        std::make_unique<PositionBodyFromJava>(
            env, position_bodyObject);
    value.position_body = position_bodyValue->value;
    env->DeleteLocalRef(position_bodyObject);
    jfieldID angle_bodyField = env->GetFieldID(
        clazz, "angleBody", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$AngleBody;");
    jobject angle_bodyObject =
        env->GetObjectField(object, angle_bodyField);
    angle_bodyValue =
        std::make_unique<AngleBodyFromJava>(
            env, angle_bodyObject);
    value.angle_body = angle_bodyValue->value;
    env->DeleteLocalRef(angle_bodyObject);
    jfieldID pose_covarianceField = env->GetFieldID(
        clazz, "poseCovariance", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;");
    jobject pose_covarianceObject =
        env->GetObjectField(object, pose_covarianceField);
    pose_covarianceValue =
        std::make_unique<CovarianceFromJava>(
            env, pose_covarianceObject);
    value.pose_covariance = pose_covarianceValue->value;
    env->DeleteLocalRef(pose_covarianceObject);
    env->DeleteLocalRef(clazz);
}

VisionPositionEstimateFromJava::~VisionPositionEstimateFromJava() = default;
VisionSpeedEstimateFromJava::VisionSpeedEstimateFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID time_usecField = env->GetFieldID(
        clazz, "timeUsec", "J");
    value.time_usec =
        static_cast<uint64_t>(env->GetLongField(object, time_usecField));
    jfieldID speed_nedField = env->GetFieldID(
        clazz, "speedNed", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$SpeedNed;");
    jobject speed_nedObject =
        env->GetObjectField(object, speed_nedField);
    speed_nedValue =
        std::make_unique<SpeedNedFromJava>(
            env, speed_nedObject);
    value.speed_ned = speed_nedValue->value;
    env->DeleteLocalRef(speed_nedObject);
    jfieldID speed_covarianceField = env->GetFieldID(
        clazz, "speedCovariance", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;");
    jobject speed_covarianceObject =
        env->GetObjectField(object, speed_covarianceField);
    speed_covarianceValue =
        std::make_unique<CovarianceFromJava>(
            env, speed_covarianceObject);
    value.speed_covariance = speed_covarianceValue->value;
    env->DeleteLocalRef(speed_covarianceObject);
    env->DeleteLocalRef(clazz);
}

VisionSpeedEstimateFromJava::~VisionSpeedEstimateFromJava() = default;
AttitudePositionMocapFromJava::AttitudePositionMocapFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID time_usecField = env->GetFieldID(
        clazz, "timeUsec", "J");
    value.time_usec =
        static_cast<uint64_t>(env->GetLongField(object, time_usecField));
    jfieldID qField = env->GetFieldID(
        clazz, "q", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Quaternion;");
    jobject qObject =
        env->GetObjectField(object, qField);
    qValue =
        std::make_unique<QuaternionFromJava>(
            env, qObject);
    value.q = qValue->value;
    env->DeleteLocalRef(qObject);
    jfieldID position_bodyField = env->GetFieldID(
        clazz, "positionBody", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody;");
    jobject position_bodyObject =
        env->GetObjectField(object, position_bodyField);
    position_bodyValue =
        std::make_unique<PositionBodyFromJava>(
            env, position_bodyObject);
    value.position_body = position_bodyValue->value;
    env->DeleteLocalRef(position_bodyObject);
    jfieldID pose_covarianceField = env->GetFieldID(
        clazz, "poseCovariance", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;");
    jobject pose_covarianceObject =
        env->GetObjectField(object, pose_covarianceField);
    pose_covarianceValue =
        std::make_unique<CovarianceFromJava>(
            env, pose_covarianceObject);
    value.pose_covariance = pose_covarianceValue->value;
    env->DeleteLocalRef(pose_covarianceObject);
    env->DeleteLocalRef(clazz);
}

AttitudePositionMocapFromJava::~AttitudePositionMocapFromJava() = default;
OdometryFromJava::OdometryFromJava(JNIEnv* env, jobject object) {
    if (!object) {
        return;
    }
    jclass clazz = env->GetObjectClass(object);
    jfieldID time_usecField = env->GetFieldID(
        clazz, "timeUsec", "J");
    value.time_usec =
        static_cast<uint64_t>(env->GetLongField(object, time_usecField));
    jfieldID frame_idField = env->GetFieldID(
        clazz, "frameId", "I");
    value.frame_id =
        static_cast<mavsdk_mocap_odometry_mav_frame_t>(env->GetIntField(object, frame_idField));
    jfieldID position_bodyField = env->GetFieldID(
        clazz, "positionBody", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody;");
    jobject position_bodyObject =
        env->GetObjectField(object, position_bodyField);
    position_bodyValue =
        std::make_unique<PositionBodyFromJava>(
            env, position_bodyObject);
    value.position_body = position_bodyValue->value;
    env->DeleteLocalRef(position_bodyObject);
    jfieldID qField = env->GetFieldID(
        clazz, "q", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Quaternion;");
    jobject qObject =
        env->GetObjectField(object, qField);
    qValue =
        std::make_unique<QuaternionFromJava>(
            env, qObject);
    value.q = qValue->value;
    env->DeleteLocalRef(qObject);
    jfieldID speed_bodyField = env->GetFieldID(
        clazz, "speedBody", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$SpeedBody;");
    jobject speed_bodyObject =
        env->GetObjectField(object, speed_bodyField);
    speed_bodyValue =
        std::make_unique<SpeedBodyFromJava>(
            env, speed_bodyObject);
    value.speed_body = speed_bodyValue->value;
    env->DeleteLocalRef(speed_bodyObject);
    jfieldID angular_velocity_bodyField = env->GetFieldID(
        clazz, "angularVelocityBody", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$AngularVelocityBody;");
    jobject angular_velocity_bodyObject =
        env->GetObjectField(object, angular_velocity_bodyField);
    angular_velocity_bodyValue =
        std::make_unique<AngularVelocityBodyFromJava>(
            env, angular_velocity_bodyObject);
    value.angular_velocity_body = angular_velocity_bodyValue->value;
    env->DeleteLocalRef(angular_velocity_bodyObject);
    jfieldID pose_covarianceField = env->GetFieldID(
        clazz, "poseCovariance", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;");
    jobject pose_covarianceObject =
        env->GetObjectField(object, pose_covarianceField);
    pose_covarianceValue =
        std::make_unique<CovarianceFromJava>(
            env, pose_covarianceObject);
    value.pose_covariance = pose_covarianceValue->value;
    env->DeleteLocalRef(pose_covarianceObject);
    jfieldID velocity_covarianceField = env->GetFieldID(
        clazz, "velocityCovariance", "Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;");
    jobject velocity_covarianceObject =
        env->GetObjectField(object, velocity_covarianceField);
    velocity_covarianceValue =
        std::make_unique<CovarianceFromJava>(
            env, velocity_covarianceObject);
    value.velocity_covariance = velocity_covarianceValue->value;
    env->DeleteLocalRef(velocity_covarianceObject);
    env->DeleteLocalRef(clazz);
}

OdometryFromJava::~OdometryFromJava() = default;

jobject toJavaPositionBody(
    JNIEnv* env, const mavsdk_mocap_position_body_t& value);
jobjectArray toJavaPositionBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_position_body_t* values,
    size_t count);
jobject toJavaAngleBody(
    JNIEnv* env, const mavsdk_mocap_angle_body_t& value);
jobjectArray toJavaAngleBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_angle_body_t* values,
    size_t count);
jobject toJavaSpeedBody(
    JNIEnv* env, const mavsdk_mocap_speed_body_t& value);
jobjectArray toJavaSpeedBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_speed_body_t* values,
    size_t count);
jobject toJavaSpeedNed(
    JNIEnv* env, const mavsdk_mocap_speed_ned_t& value);
jobjectArray toJavaSpeedNedArray(
    JNIEnv* env,
    const mavsdk_mocap_speed_ned_t* values,
    size_t count);
jobject toJavaAngularVelocityBody(
    JNIEnv* env, const mavsdk_mocap_angular_velocity_body_t& value);
jobjectArray toJavaAngularVelocityBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_angular_velocity_body_t* values,
    size_t count);
jobject toJavaCovariance(
    JNIEnv* env, const mavsdk_mocap_covariance_t& value);
jobjectArray toJavaCovarianceArray(
    JNIEnv* env,
    const mavsdk_mocap_covariance_t* values,
    size_t count);
jobject toJavaQuaternion(
    JNIEnv* env, const mavsdk_mocap_quaternion_t& value);
jobjectArray toJavaQuaternionArray(
    JNIEnv* env,
    const mavsdk_mocap_quaternion_t* values,
    size_t count);
jobject toJavaVisionPositionEstimate(
    JNIEnv* env, const mavsdk_mocap_vision_position_estimate_t& value);
jobjectArray toJavaVisionPositionEstimateArray(
    JNIEnv* env,
    const mavsdk_mocap_vision_position_estimate_t* values,
    size_t count);
jobject toJavaVisionSpeedEstimate(
    JNIEnv* env, const mavsdk_mocap_vision_speed_estimate_t& value);
jobjectArray toJavaVisionSpeedEstimateArray(
    JNIEnv* env,
    const mavsdk_mocap_vision_speed_estimate_t* values,
    size_t count);
jobject toJavaAttitudePositionMocap(
    JNIEnv* env, const mavsdk_mocap_attitude_position_mocap_t& value);
jobjectArray toJavaAttitudePositionMocapArray(
    JNIEnv* env,
    const mavsdk_mocap_attitude_position_mocap_t* values,
    size_t count);
jobject toJavaOdometry(
    JNIEnv* env, const mavsdk_mocap_odometry_t& value);
jobjectArray toJavaOdometryArray(
    JNIEnv* env,
    const mavsdk_mocap_odometry_t* values,
    size_t count);

jobject toJavaPositionBody(
    JNIEnv* env, const mavsdk_mocap_position_body_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(FFF)V");
    if (!constructor) {
        return nullptr;
    }
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jfloat>(value.x_m)
        , static_cast<jfloat>(value.y_m)
        , static_cast<jfloat>(value.z_m)
    );
    return result;
}

jobjectArray toJavaPositionBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_position_body_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaPositionBody(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaAngleBody(
    JNIEnv* env, const mavsdk_mocap_angle_body_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$AngleBody");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(FFF)V");
    if (!constructor) {
        return nullptr;
    }
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jfloat>(value.roll_rad)
        , static_cast<jfloat>(value.pitch_rad)
        , static_cast<jfloat>(value.yaw_rad)
    );
    return result;
}

jobjectArray toJavaAngleBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_angle_body_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$AngleBody");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaAngleBody(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaSpeedBody(
    JNIEnv* env, const mavsdk_mocap_speed_body_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$SpeedBody");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(FFF)V");
    if (!constructor) {
        return nullptr;
    }
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jfloat>(value.x_m_s)
        , static_cast<jfloat>(value.y_m_s)
        , static_cast<jfloat>(value.z_m_s)
    );
    return result;
}

jobjectArray toJavaSpeedBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_speed_body_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$SpeedBody");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaSpeedBody(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaSpeedNed(
    JNIEnv* env, const mavsdk_mocap_speed_ned_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$SpeedNed");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(FFF)V");
    if (!constructor) {
        return nullptr;
    }
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jfloat>(value.north_m_s)
        , static_cast<jfloat>(value.east_m_s)
        , static_cast<jfloat>(value.down_m_s)
    );
    return result;
}

jobjectArray toJavaSpeedNedArray(
    JNIEnv* env,
    const mavsdk_mocap_speed_ned_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$SpeedNed");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaSpeedNed(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaAngularVelocityBody(
    JNIEnv* env, const mavsdk_mocap_angular_velocity_body_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$AngularVelocityBody");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(FFF)V");
    if (!constructor) {
        return nullptr;
    }
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jfloat>(value.roll_rad_s)
        , static_cast<jfloat>(value.pitch_rad_s)
        , static_cast<jfloat>(value.yaw_rad_s)
    );
    return result;
}

jobjectArray toJavaAngularVelocityBodyArray(
    JNIEnv* env,
    const mavsdk_mocap_angular_velocity_body_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$AngularVelocityBody");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaAngularVelocityBody(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaCovariance(
    JNIEnv* env, const mavsdk_mocap_covariance_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$Covariance");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "([F)V");
    if (!constructor) {
        return nullptr;
    }
    jfloatArray covariance_matrixValue =
        env->NewFloatArray(
            static_cast<jsize>(value.covariance_matrix_size));
    std::vector<jfloat> covariance_matrixJavaValues;
    covariance_matrixJavaValues.reserve(value.covariance_matrix_size);
    for (size_t i = 0; i < value.covariance_matrix_size; ++i) {
        covariance_matrixJavaValues.push_back(
            static_cast<jfloat>(value.covariance_matrix[i]));
    }
    if (!covariance_matrixJavaValues.empty()) {
        env->SetFloatArrayRegion(
            covariance_matrixValue, 0,
            static_cast<jsize>(covariance_matrixJavaValues.size()),
            covariance_matrixJavaValues.data());
    }
    jobject result = env->NewObject(carrierClass, constructor
        , covariance_matrixValue
    );
    env->DeleteLocalRef(covariance_matrixValue);
    return result;
}

jobjectArray toJavaCovarianceArray(
    JNIEnv* env,
    const mavsdk_mocap_covariance_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$Covariance");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaCovariance(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaQuaternion(
    JNIEnv* env, const mavsdk_mocap_quaternion_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$Quaternion");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(FFFF)V");
    if (!constructor) {
        return nullptr;
    }
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jfloat>(value.w)
        , static_cast<jfloat>(value.x)
        , static_cast<jfloat>(value.y)
        , static_cast<jfloat>(value.z)
    );
    return result;
}

jobjectArray toJavaQuaternionArray(
    JNIEnv* env,
    const mavsdk_mocap_quaternion_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$Quaternion");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaQuaternion(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaVisionPositionEstimate(
    JNIEnv* env, const mavsdk_mocap_vision_position_estimate_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$VisionPositionEstimate");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(JLio/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody;Lio/mavsdk/jni/plugins/mocap/NativeMocap$AngleBody;Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;)V");
    if (!constructor) {
        return nullptr;
    }
    jobject position_bodyValue =
        toJavaPositionBody(env, value.position_body);
    jobject angle_bodyValue =
        toJavaAngleBody(env, value.angle_body);
    jobject pose_covarianceValue =
        toJavaCovariance(env, value.pose_covariance);
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jlong>(value.time_usec)
        , position_bodyValue
        , angle_bodyValue
        , pose_covarianceValue
    );
    env->DeleteLocalRef(position_bodyValue);
    env->DeleteLocalRef(angle_bodyValue);
    env->DeleteLocalRef(pose_covarianceValue);
    return result;
}

jobjectArray toJavaVisionPositionEstimateArray(
    JNIEnv* env,
    const mavsdk_mocap_vision_position_estimate_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$VisionPositionEstimate");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaVisionPositionEstimate(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaVisionSpeedEstimate(
    JNIEnv* env, const mavsdk_mocap_vision_speed_estimate_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$VisionSpeedEstimate");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(JLio/mavsdk/jni/plugins/mocap/NativeMocap$SpeedNed;Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;)V");
    if (!constructor) {
        return nullptr;
    }
    jobject speed_nedValue =
        toJavaSpeedNed(env, value.speed_ned);
    jobject speed_covarianceValue =
        toJavaCovariance(env, value.speed_covariance);
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jlong>(value.time_usec)
        , speed_nedValue
        , speed_covarianceValue
    );
    env->DeleteLocalRef(speed_nedValue);
    env->DeleteLocalRef(speed_covarianceValue);
    return result;
}

jobjectArray toJavaVisionSpeedEstimateArray(
    JNIEnv* env,
    const mavsdk_mocap_vision_speed_estimate_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$VisionSpeedEstimate");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaVisionSpeedEstimate(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaAttitudePositionMocap(
    JNIEnv* env, const mavsdk_mocap_attitude_position_mocap_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$AttitudePositionMocap");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(JLio/mavsdk/jni/plugins/mocap/NativeMocap$Quaternion;Lio/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody;Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;)V");
    if (!constructor) {
        return nullptr;
    }
    jobject qValue =
        toJavaQuaternion(env, value.q);
    jobject position_bodyValue =
        toJavaPositionBody(env, value.position_body);
    jobject pose_covarianceValue =
        toJavaCovariance(env, value.pose_covariance);
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jlong>(value.time_usec)
        , qValue
        , position_bodyValue
        , pose_covarianceValue
    );
    env->DeleteLocalRef(qValue);
    env->DeleteLocalRef(position_bodyValue);
    env->DeleteLocalRef(pose_covarianceValue);
    return result;
}

jobjectArray toJavaAttitudePositionMocapArray(
    JNIEnv* env,
    const mavsdk_mocap_attitude_position_mocap_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$AttitudePositionMocap");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaAttitudePositionMocap(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}
jobject toJavaOdometry(
    JNIEnv* env, const mavsdk_mocap_odometry_t& value) {
    jclass carrierClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$Odometry");
    if (!carrierClass) {
        return nullptr;
    }
    jmethodID constructor = env->GetMethodID(
        carrierClass, "<init>", "(JILio/mavsdk/jni/plugins/mocap/NativeMocap$PositionBody;Lio/mavsdk/jni/plugins/mocap/NativeMocap$Quaternion;Lio/mavsdk/jni/plugins/mocap/NativeMocap$SpeedBody;Lio/mavsdk/jni/plugins/mocap/NativeMocap$AngularVelocityBody;Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;Lio/mavsdk/jni/plugins/mocap/NativeMocap$Covariance;)V");
    if (!constructor) {
        return nullptr;
    }
    jobject position_bodyValue =
        toJavaPositionBody(env, value.position_body);
    jobject qValue =
        toJavaQuaternion(env, value.q);
    jobject speed_bodyValue =
        toJavaSpeedBody(env, value.speed_body);
    jobject angular_velocity_bodyValue =
        toJavaAngularVelocityBody(env, value.angular_velocity_body);
    jobject pose_covarianceValue =
        toJavaCovariance(env, value.pose_covariance);
    jobject velocity_covarianceValue =
        toJavaCovariance(env, value.velocity_covariance);
    jobject result = env->NewObject(carrierClass, constructor
        , static_cast<jlong>(value.time_usec)
        , static_cast<jint>(value.frame_id)
        , position_bodyValue
        , qValue
        , speed_bodyValue
        , angular_velocity_bodyValue
        , pose_covarianceValue
        , velocity_covarianceValue
    );
    env->DeleteLocalRef(position_bodyValue);
    env->DeleteLocalRef(qValue);
    env->DeleteLocalRef(speed_bodyValue);
    env->DeleteLocalRef(angular_velocity_bodyValue);
    env->DeleteLocalRef(pose_covarianceValue);
    env->DeleteLocalRef(velocity_covarianceValue);
    return result;
}

jobjectArray toJavaOdometryArray(
    JNIEnv* env,
    const mavsdk_mocap_odometry_t* values,
    size_t count) {
    jclass elementClass = findClass(env, "io/mavsdk/jni/plugins/mocap/NativeMocap$Odometry");
    if (!elementClass) {
        return nullptr;
    }
    jobjectArray result = env->NewObjectArray(static_cast<jsize>(count), elementClass, nullptr);
    for (size_t i = 0; i < count; ++i) {
        jobject item = toJavaOdometry(env, values[i]);
        env->SetObjectArrayElement(result, static_cast<jsize>(i), item);
        env->DeleteLocalRef(item);
    }
    return result;
}


} // namespace

extern "C" {

JNIEXPORT jlong JNICALL
Java_io_mavsdk_jni_plugins_mocap_NativeMocap_create(JNIEnv* env, jclass, jlong systemHandle) {
    if (!systemHandle) {
        throwMavsdkError(env, "OperationError", "Invalid system handle");
        return 0;
    }
    mavsdk_mocap_t handle = mavsdk_mocap_create(
        reinterpret_cast<mavsdk_system_t>(systemHandle)
    );
    if (!handle) {
        throwMavsdkError(env, "OperationError", "Failed to create Mocap plugin");
        return 0;
    }
    return reinterpret_cast<jlong>(handle);
}

JNIEXPORT void JNICALL
Java_io_mavsdk_jni_plugins_mocap_NativeMocap_destroy(JNIEnv* env, jclass, jlong handle) {
    if (!requireHandle(env, handle, "Mocap plugin")) {
        return;
    }
    mavsdk_mocap_destroy(
        reinterpret_cast<mavsdk_mocap_t>(handle));
}

JNIEXPORT
jint
JNICALL Java_io_mavsdk_jni_plugins_mocap_NativeMocap_setVisionPositionEstimate(
    JNIEnv* env,
    jclass,
    jlong handle,
    jobject vision_position_estimate) {
    if (!requireHandle(env, handle, "Mocap plugin")) {
        return {};
    }
    VisionPositionEstimateFromJava
        vision_position_estimateValue(env, vision_position_estimate);
    mavsdk_mocap_result_t result =
        mavsdk_mocap_set_vision_position_estimate(
            reinterpret_cast<mavsdk_mocap_t>(handle),
            vision_position_estimateValue.value);
    return static_cast<jint>(result);
}

JNIEXPORT
jint
JNICALL Java_io_mavsdk_jni_plugins_mocap_NativeMocap_setVisionSpeedEstimate(
    JNIEnv* env,
    jclass,
    jlong handle,
    jobject vision_speed_estimate) {
    if (!requireHandle(env, handle, "Mocap plugin")) {
        return {};
    }
    VisionSpeedEstimateFromJava
        vision_speed_estimateValue(env, vision_speed_estimate);
    mavsdk_mocap_result_t result =
        mavsdk_mocap_set_vision_speed_estimate(
            reinterpret_cast<mavsdk_mocap_t>(handle),
            vision_speed_estimateValue.value);
    return static_cast<jint>(result);
}

JNIEXPORT
jint
JNICALL Java_io_mavsdk_jni_plugins_mocap_NativeMocap_setAttitudePositionMocap(
    JNIEnv* env,
    jclass,
    jlong handle,
    jobject attitude_position_mocap) {
    if (!requireHandle(env, handle, "Mocap plugin")) {
        return {};
    }
    AttitudePositionMocapFromJava
        attitude_position_mocapValue(env, attitude_position_mocap);
    mavsdk_mocap_result_t result =
        mavsdk_mocap_set_attitude_position_mocap(
            reinterpret_cast<mavsdk_mocap_t>(handle),
            attitude_position_mocapValue.value);
    return static_cast<jint>(result);
}

JNIEXPORT
jint
JNICALL Java_io_mavsdk_jni_plugins_mocap_NativeMocap_setOdometry(
    JNIEnv* env,
    jclass,
    jlong handle,
    jobject odometry) {
    if (!requireHandle(env, handle, "Mocap plugin")) {
        return {};
    }
    OdometryFromJava
        odometryValue(env, odometry);
    mavsdk_mocap_result_t result =
        mavsdk_mocap_set_odometry(
            reinterpret_cast<mavsdk_mocap_t>(handle),
            odometryValue.value);
    return static_cast<jint>(result);
}

} // extern "C"