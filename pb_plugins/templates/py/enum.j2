class {{ name }}:
    """ Generated by dcsdkgen """

    @staticmethod
    def parse_response(response):
        """ Parses a gRPC response """
        return {
            {%- for value in values %}
                {{ loop.index - 1 }}: "{{ value }}",
            {%- endfor %}
            }.get(response.{{ letter_case_to_delimiter(name) }}, default=None)
