
async def {{ name.lower() }}(self):
    """ Generated by dcsdkgen """
    async for response in self._stub.Subscribe{{ name }}(
                {{ plugin_name.lower() }}_pb2.Subscribe{{ name }}Request()):
        {%- if is_return_type_primitive %}
            yield response.{{ letter_case_to_delimiter(return_name) }}
        {%- else %}
            yield {{ return_type }}.parse_response(response)
        {%- endif -%}
