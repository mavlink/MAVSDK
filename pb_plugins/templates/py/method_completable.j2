
def {{ name }}(self):
    """ Generated by dcsdkgen

    :returns: Observable
    """

    def f(observable):
        response = self._stub.{{ name }}({{ plugin_name.lower() }}_pb2.{{ name }}Request())

        if self._response_success(response):
            observable.on_completed()
        else:
            observable.on_error(response)

    return self._create_call_observable(f)
