// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/manual_control/manual_control.proto)

package io.mavsdk.kotlin.plugins.manual_control

import io.mavsdk.jni.plugins.manual_control.NativeManualControl
import java.util.concurrent.atomic.AtomicBoolean

private class ManualControlNativeImpl(
    private val handle: Long
) : ManualControlNative {
    override fun startPositionControlAsync(
        callback: (Int) -> Unit
    ) {
        NativeManualControl.startPositionControlAsync(
            handle,
            NativeManualControl.StartPositionControlCallback {
                    result -> callback(result)
            }
        )
    }

    override fun startAltitudeControlAsync(
        callback: (Int) -> Unit
    ) {
        NativeManualControl.startAltitudeControlAsync(
            handle,
            NativeManualControl.StartAltitudeControlCallback {
                    result -> callback(result)
            }
        )
    }

    override fun setManualControlInput(x: Float, y: Float, z: Float, r: Float): Int =
        NativeManualControl.setManualControlInput(handle, x, y, z, r)

    override fun destroy() {
        NativeManualControl.destroy(handle)
    }
}

internal actual fun createManualControlNative(systemHandle: Long): ManualControlNative =
    ManualControlNativeImpl(NativeManualControl.create(systemHandle))

internal actual class ManualControlCallbackGuard actual constructor() {
    private val claimed = AtomicBoolean(false)

    actual fun tryClaim(): Boolean = claimed.compareAndSet(false, true)
}
