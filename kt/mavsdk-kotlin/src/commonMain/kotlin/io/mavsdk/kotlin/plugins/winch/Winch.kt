// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/winch/winch.proto)

package io.mavsdk.kotlin.plugins.winch

import io.mavsdk.kotlin.System
import kotlin.coroutines.resume
import kotlinx.coroutines.channels.awaitClose
import kotlinx.coroutines.flow.Flow
import kotlinx.coroutines.flow.callbackFlow
import kotlinx.coroutines.suspendCancellableCoroutine

/** Allows users to send winch actions, as well as receive status information from winch systems. */
class Winch internal constructor(private val native: WinchNative) : AutoCloseable {
    private var closed = false

    /** Possible results returned for winch action requests. */
    enum class Result(val value: Int) {
        /** Unknown result */
        UNKNOWN(0),
        /** Request was successful */
        SUCCESS(1),
        /** No system is connected */
        NO_SYSTEM(2),
        /** Temporarily rejected */
        BUSY(3),
        /** Request timed out */
        TIMEOUT(4),
        /** Action not supported */
        UNSUPPORTED(5),
        /** Action failed */
        FAILED(6);

        companion object {
            fun fromValue(value: Int): Result = entries.find { it.value == value } ?: UNKNOWN
        }
    }

    /** Winch Action type. */
    enum class WinchAction(val value: Int) {
        /** Allow motor to freewheel */
        RELAXED(0),
        /** Wind or unwind specified length of line, optionally using specified rate */
        RELATIVE_LENGTH_CONTROL(1),
        /** Wind or unwind line at specified rate */
        RATE_CONTROL(2),
        /** Perform the locking sequence to relieve motor while in the fully retracted position */
        LOCK(3),
        /** Sequence of drop, slow down, touch down, reel up, lock */
        DELIVER(4),
        /** Engage motor and hold current position */
        HOLD(5),
        /** Return the reel to the fully retracted position */
        RETRACT(6),
        /**
         * Load the reel with line. The winch will calculate the total loaded length and stop when
         * the tension exceeds a threshold
         */
        LOAD_LINE(7),
        /** Spool out the entire length of the line */
        ABANDON_LINE(8),
        /** Spools out just enough to present the hook to the user to load the payload */
        LOAD_PAYLOAD(9);

        companion object {
            fun fromValue(value: Int): WinchAction =
                entries.find { it.value == value } ?: entries.first()
        }
    }

    /**
     * Winch Status Flags.
     *
     * The status flags are defined in mavlink
     * https://mavlink.io/en/messages/common.html#MAV_WINCH_STATUS_FLAG.
     *
     * Multiple status fields can be set simultaneously. Mavlink does not specify which states are
     * mutually exclusive.
     *
     * @property healthy Winch is healthy
     * @property fullyRetracted Winch line is fully retracted
     * @property moving Winch motor is moving
     * @property clutchEngaged Winch clutch is engaged allowing motor to move freely
     * @property locked Winch is locked by locking mechanism
     * @property dropping Winch is gravity dropping payload
     * @property arresting Winch is arresting payload descent
     * @property groundSense Winch is using torque measurements to sense the ground
     * @property retracting Winch is returning to the fully retracted position
     * @property redeliver Winch is redelivering the payload. This is a failover state if the line
     *   tension goes above a threshold during RETRACTING.
     * @property abandonLine Winch is abandoning the line and possibly payload. Winch unspools the
     *   entire calculated line length. This is a failover state from REDELIVER if the number of
     *   attempts exceeds a threshold.
     * @property locking Winch is engaging the locking mechanism
     * @property loadLine Winch is spooling on line
     * @property loadPayload Winch is loading a payload
     */
    data class StatusFlags(
        val healthy: Boolean,
        val fullyRetracted: Boolean,
        val moving: Boolean,
        val clutchEngaged: Boolean,
        val locked: Boolean,
        val dropping: Boolean,
        val arresting: Boolean,
        val groundSense: Boolean,
        val retracting: Boolean,
        val redeliver: Boolean,
        val abandonLine: Boolean,
        val locking: Boolean,
        val loadLine: Boolean,
        val loadPayload: Boolean,
    )

    /**
     * Status type.
     *
     * @property timeUsec Time in usec
     * @property lineLengthM Length of the line in meters
     * @property speedMS Speed in meters per second
     * @property tensionKg Tension in kilograms
     * @property voltageV Voltage in volts
     * @property currentA Current in amperes
     * @property temperatureC Temperature in Celsius
     * @property statusFlags Status flags
     */
    data class Status(
        val timeUsec: Long,
        val lineLengthM: Float,
        val speedMS: Float,
        val tensionKg: Float,
        val voltageV: Float,
        val currentA: Float,
        val temperatureC: Int,
        val statusFlags: StatusFlags,
    )

    /**
     * Subscribe to 'winch status' updates.
     *
     * @return The next 'winch status' state
     */
    fun status(): Status = native.status()

    /**
     * Subscribe to 'winch status' updates.
     *
     * @return The next 'winch status' state
     */
    fun subscribeStatus(): Flow<Status> = callbackFlow {
        val subscriptionHandle = native.subscribeStatus() { value -> trySend(value) }
        awaitClose { native.unsubscribeStatus(subscriptionHandle) }
    }

    /**
     * Allow motor to freewheel.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun relax(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.relaxAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Wind or unwind specified length of line, optionally using specified rate.
     *
     * @param instance Instance ID of the winch addressed by this request
     * @param lengthM Length of line to unwind or wind
     * @param rateMS Rate at which to wind or unwind the line
     * @return The result of the request.
     */
    suspend fun relativeLengthControl(instance: Int, lengthM: Float, rateMS: Float): Result =
        suspendCancellableCoroutine { continuation ->
            val callbackGuard = WinchCallbackGuard()
            native.relativeLengthControlAsync(instance, lengthM, rateMS) { result ->
                val parsedResult = Result.fromValue(result)
                if (continuation.isActive && true && callbackGuard.tryClaim()) {
                    continuation.resume(parsedResult)
                }
            }
        }

    /**
     * Wind or unwind line at specified rate.
     *
     * @param instance
     * @param rateMS Rate at which to wind or unwind the line
     * @return The result of the request.
     */
    suspend fun rateControl(instance: Int, rateMS: Float): Result =
        suspendCancellableCoroutine { continuation ->
            val callbackGuard = WinchCallbackGuard()
            native.rateControlAsync(instance, rateMS) { result ->
                val parsedResult = Result.fromValue(result)
                if (continuation.isActive && true && callbackGuard.tryClaim()) {
                    continuation.resume(parsedResult)
                }
            }
        }

    /**
     * Perform the locking sequence to relieve motor while in the fully retracted position.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun lock(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.lockAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Sequence of drop, slow down, touch down, reel up, lock.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun deliver(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.deliverAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Engage motor and hold current position.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun hold(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.holdAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Return the reel to the fully retracted position.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun retract(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.retractAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Load the reel with line.
     *
     * The winch will calculate the total loaded length and stop when the tension exceeds a
     * threshold.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun loadLine(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.loadLineAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Spool out the entire length of the line.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun abandonLine(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.abandonLineAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    /**
     * Spools out just enough to present the hook to the user to load the payload.
     *
     * @param instance
     * @return The result of the request.
     */
    suspend fun loadPayload(instance: Int): Result = suspendCancellableCoroutine { continuation ->
        val callbackGuard = WinchCallbackGuard()
        native.loadPayloadAsync(instance) { result ->
            val parsedResult = Result.fromValue(result)
            if (continuation.isActive && true && callbackGuard.tryClaim()) {
                continuation.resume(parsedResult)
            }
        }
    }

    override fun close() {
        if (closed) return
        closed = true
        native.destroy()
    }

    class WinchException(val result: Result, message: String) : Exception(message)

    companion object {
        fun create(system: System): Winch =
            Winch(createWinchNative(system.getHandle())).also { system.registerPlugin(it) }
    }
}

internal interface WinchNative {
    fun status(): Winch.Status

    fun subscribeStatus(callback: (Winch.Status) -> Unit): Long

    fun unsubscribeStatus(subscriptionHandle: Long)

    fun relaxAsync(instance: Int, callback: (Int) -> Unit)

    fun relativeLengthControlAsync(
        instance: Int,
        lengthM: Float,
        rateMS: Float,
        callback: (Int) -> Unit,
    )

    fun rateControlAsync(instance: Int, rateMS: Float, callback: (Int) -> Unit)

    fun lockAsync(instance: Int, callback: (Int) -> Unit)

    fun deliverAsync(instance: Int, callback: (Int) -> Unit)

    fun holdAsync(instance: Int, callback: (Int) -> Unit)

    fun retractAsync(instance: Int, callback: (Int) -> Unit)

    fun loadLineAsync(instance: Int, callback: (Int) -> Unit)

    fun abandonLineAsync(instance: Int, callback: (Int) -> Unit)

    fun loadPayloadAsync(instance: Int, callback: (Int) -> Unit)

    fun destroy()
}

internal expect fun createWinchNative(systemHandle: Long): WinchNative

internal expect class WinchCallbackGuard() {
    fun tryClaim(): Boolean
}
