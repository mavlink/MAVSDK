// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/camera_trigger_server/camera_trigger_server.proto)

#pragma once

#include <array>
#include <cmath>
#include <functional>
#include <limits>
#include <memory>
#include <string>
#include <utility>
#include <vector>

#include "plugin_base.h"

namespace mavsdk {

class System;
class CameraTriggerServerImpl;

/**
 * @brief Provides handling of camera trigger commands.
 */
class CameraTriggerServer : public PluginBase {
public:
    /**
     * @brief Constructor. Creates the plugin for a specific System.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto camera_trigger_server = CameraTriggerServer(system);
     *     ```
     *
     * @param system The specific system associated with this plugin.
     */
    explicit CameraTriggerServer(System& system); // deprecated

    /**
     * @brief Constructor. Creates the plugin for a specific System.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto camera_trigger_server = CameraTriggerServer(system);
     *     ```
     *
     * @param system The specific system associated with this plugin.
     */
    explicit CameraTriggerServer(std::shared_ptr<System> system); // new

    /**
     * @brief Destructor (internal use only).
     */
    ~CameraTriggerServer();






    /**
     * @brief Possible results returned for action requests.
     */
    enum class Result {
        Unknown, /**< @brief Unknown result. */
        Success, /**< @brief Success: the telemetry command was accepted by the vehicle. */
        NoSystem, /**< @brief No system connected. */
        ConnectionError, /**< @brief Connection error. */
        Busy, /**< @brief Vehicle is busy. */
        CommandDenied, /**< @brief Command refused by vehicle. */
        Timeout, /**< @brief Request timed out. */
        Unsupported, /**< @brief Request not supported. */
    };

    /**
     * @brief Stream operator to print information about a `CameraTriggerServer::Result`.
     *
     * @return A reference to the stream.
     */
    friend std::ostream& operator<<(std::ostream& str, CameraTriggerServer::Result const& result);



    /**
     * @brief Callback type for asynchronous CameraTriggerServer calls.
     */
    using ResultCallback = std::function<void(Result)>;




        
    /**
    * @brief Callback type for subscribe_capture.
    */
        
    using CaptureCallback = std::function<void(Result, uint32_t)>;

    /**
     * @brief Subscribe to single-capture MAV_CMD_IMAGE_START_CAPTURE commands
     */
    void subscribe_capture(CaptureCallback callback);






    /**
     * @brief Copy constructor.
     */
    CameraTriggerServer(const CameraTriggerServer& other);

    /**
     * @brief Equality operator (object is not copyable).
     */
    const CameraTriggerServer& operator=(const CameraTriggerServer&) = delete;

private:
    /** @private Underlying implementation, set at instantiation */
    std::unique_ptr<CameraTriggerServerImpl> _impl;
};

} // namespace mavsdk