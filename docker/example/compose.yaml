services:
  mavsdk:
    build:
      context: ../../
      dockerfile: ./docker/example/Dockerfile
      args:
        MAVLINK_DIALECT: ${MAVLINK_DIALECT:-}
        BUILD_THREADS: ${BUILD_THREADS:-6}
    command: ${MAV_CONNECTION:-}
    network_mode: "host"
