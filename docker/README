These Dockerfiles will create a DronecodeSDK development container image based on whichever flavour of Ubuntu or Fedora you prefer.

To create an image :-

cd /wherever/DronecodeSDK/docker
docker build -t dronecode -f Dockerfile-Ubuntu-18.04 .

To use the image :-

cd /wherever/DronecodeSDK
./run-docker

This will create and run a container based on your image.
/path/DronecodeSDK will be mounted inside the container at /home/dronecode/DronecodeSDK

dronecode@dronecode-docker:~/DronecodeSDK$ ls
CMakeLists.txt  Makefile      astylerc  cmake               debug_helpers  external_example  generate_markdown_from_doxygen_xml.py  plugins            stop_px4_sitl.sh        version
Jenkinsfile     README.md     backend   core                docker         fix_style.sh      iOS.cmake                              run-docker.sh      third_party
LICENSE.md      appveyor.yml  build     create_packages.sh  example        generate_docs.sh  integration_tests                      start_px4_sitl.sh  travis-docker-build.sh
dronecode@dronecode-docker:~/DronecodeSDK$ make default (or whatever)