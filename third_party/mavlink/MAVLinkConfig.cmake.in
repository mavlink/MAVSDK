get_filename_component(MAVLINK_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include("${MAVLINK_CMAKE_DIR}/MAVLinkTargets.cmake")

get_target_property(MAVLINK_HEADERS
    MAVLink::mavlink
    INTERFACE_INCLUDE_DIRECTORIES
    )

