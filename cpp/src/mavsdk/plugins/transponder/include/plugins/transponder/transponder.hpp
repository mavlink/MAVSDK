// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/transponder/transponder.proto)

#pragma once

#include <array>
#include <cmath>
#include <functional>
#include <limits>
#include <memory>
#include <string>
#include <utility>
#include <vector>


#include "plugin_base.hpp"

#include "handle.hpp"
#include "mavsdk_export.h"

namespace mavsdk {


class System;class TransponderImpl;

/**
 * @brief Allow users to get ADS-B information
 * and set ADS-B update rates.
 */
class MAVSDK_PUBLIC Transponder : public PluginBase {
public:

    /**
     * @brief Constructor. Creates the plugin for a specific System.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto transponder = Transponder(system);
     *     ```
     *
     * @param system The specific system associated with this plugin.
     */
    explicit Transponder(System& system); // deprecated

    /**
     * @brief Constructor. Creates the plugin for a specific System.
     *
     * The plugin is typically created as shown below:
     *
     *     ```cpp
     *     auto transponder = Transponder(system);
     *     ```
     *
     * @param system The specific system associated with this plugin.
     */
    explicit Transponder(std::shared_ptr<System> system); // new


    /**
     * @brief Destructor (internal use only).
     */
    ~Transponder() override;


    /**
     * @brief ADSB classification for the type of vehicle emitting the transponder signal.
     */
    enum class AdsbEmitterType {
        NoInfo = 0, /**< @brief No emitter info.. */
        Light = 1, /**< @brief Light emitter.. */
        Small = 2, /**< @brief Small emitter.. */
        Large = 3, /**< @brief Large emitter.. */
        HighVortexLarge = 4, /**< @brief High vortex emitter.. */
        Heavy = 5, /**< @brief Heavy emitter.. */
        HighlyManuv = 6, /**< @brief Highly maneuverable emitter.. */
        Rotocraft = 7, /**< @brief Rotorcraft emitter.. */
        Unassigned = 8, /**< @brief Unassigned emitter.. */
        Glider = 9, /**< @brief Glider emitter.. */
        LighterAir = 10, /**< @brief Lighter air emitter.. */
        Parachute = 11, /**< @brief Parachute emitter.. */
        UltraLight = 12, /**< @brief Ultra light emitter.. */
        Unassigned2 = 13, /**< @brief Unassigned2 emitter.. */
        Uav = 14, /**< @brief UAV emitter.. */
        Space = 15, /**< @brief Space emitter.. */
        Unassgined3 = 16, /**< @brief Unassigned3 emitter.. */
        EmergencySurface = 17, /**< @brief Emergency emitter.. */
        ServiceSurface = 18, /**< @brief Service surface emitter.. */
        PointObstacle = 19, /**< @brief Point obstacle emitter.. */
    };

    /**
     * @brief Convert `Transponder::AdsbEmitterType` to string.
     *
     * @return A string representation of the enum.
     */
    friend MAVSDK_PUBLIC std::string_view to_string(Transponder::AdsbEmitterType const& adsb_emitter_type);

    /**
     * @brief Stream operator to print information about a `Transponder::AdsbEmitterType`.
     *
     * @return A reference to the stream.
     */
    friend MAVSDK_PUBLIC std::ostream& operator<<(std::ostream& str, Transponder::AdsbEmitterType const& adsb_emitter_type);

    /**
     * @brief Altitude type used in AdsbVehicle message
     */
    enum class AdsbAltitudeType {
        PressureQnh = 0, /**< @brief Altitude reported from a Baro source using QNH reference. */
        Geometric = 1, /**< @brief Altitude reported from a GNSS source. */
    };

    /**
     * @brief Convert `Transponder::AdsbAltitudeType` to string.
     *
     * @return A string representation of the enum.
     */
    friend MAVSDK_PUBLIC std::string_view to_string(Transponder::AdsbAltitudeType const& adsb_altitude_type);

    /**
     * @brief Stream operator to print information about a `Transponder::AdsbAltitudeType`.
     *
     * @return A reference to the stream.
     */
    friend MAVSDK_PUBLIC std::ostream& operator<<(std::ostream& str, Transponder::AdsbAltitudeType const& adsb_altitude_type);




    /**
     * @brief ADSB Vehicle type.
     */
    struct AdsbVehicle {
        
        uint32_t icao_address{}; /**< @brief ICAO (International Civil Aviation Organization) unique worldwide identifier */
        double latitude_deg{}; /**< @brief Latitude in degrees (range: -90 to +90) */
        double longitude_deg{}; /**< @brief Longitude in degrees (range: -180 to +180). */
        AdsbAltitudeType altitude_type{}; /**< @brief ADSB altitude type. */
        float absolute_altitude_m{}; /**< @brief Altitude in metres according to altitude_type */
        float heading_deg{}; /**< @brief Course over ground, in degrees */
        float horizontal_velocity_m_s{}; /**< @brief The horizontal velocity in metres/second */
        float vertical_velocity_m_s{}; /**< @brief The vertical velocity in metres/second. Positive is up. */
        std::string callsign{}; /**< @brief The callsign */
        AdsbEmitterType emitter_type{}; /**< @brief ADSB emitter type. */
        uint32_t squawk{}; /**< @brief Squawk code. */
        uint32_t tslc_s{}; /**< @brief Time Since Last Communication in seconds. */
    };

    /**
     * @brief Equal operator to compare two `Transponder::AdsbVehicle` objects.
     *
     * @return `true` if items are equal.
     */
    friend MAVSDK_PUBLIC bool operator==(const Transponder::AdsbVehicle& lhs, const Transponder::AdsbVehicle& rhs);

    /**
     * @brief Stream operator to print information about a `Transponder::AdsbVehicle`.
     *
     * @return A reference to the stream.
     */
    friend MAVSDK_PUBLIC std::ostream& operator<<(std::ostream& str, Transponder::AdsbVehicle const& adsb_vehicle);





    /**
     * @brief Possible results returned for transponder requests.
     */
    enum class Result {
        Unknown = 0, /**< @brief Unknown result. */
        Success = 1, /**< @brief Success: the transponder command was accepted by the vehicle. */
        NoSystem = 2, /**< @brief No system connected. */
        ConnectionError = 3, /**< @brief Connection error. */
        Busy = 4, /**< @brief Vehicle is busy. */
        CommandDenied = 5, /**< @brief Command refused by vehicle. */
        Timeout = 6, /**< @brief Request timed out. */
    };

    /**
     * @brief Convert `Transponder::Result` to string.
     *
     * @return A string representation of the enum.
     */
    friend MAVSDK_PUBLIC std::string_view to_string(Transponder::Result const& result);

    /**
     * @brief Stream operator to print information about a `Transponder::Result`.
     *
     * @return A reference to the stream.
     */
    friend MAVSDK_PUBLIC std::ostream& operator<<(std::ostream& str, Transponder::Result const& result);



    /**
     * @brief Callback type for asynchronous Transponder calls.
     */
    using ResultCallback = std::function<void(Result)>;




        

    /**
     * @brief Callback type for subscribe_transponder.
     */
    using TransponderCallback = std::function<void(AdsbVehicle)>;

    /**
     * @brief Handle type for subscribe_transponder.
     */
    using TransponderHandle = Handle<AdsbVehicle>;

    /**
     * @brief Subscribe to 'transponder' updates.
     */
    TransponderHandle subscribe_transponder(const TransponderCallback& callback);

    /**
     * @brief Unsubscribe from subscribe_transponder
     */
    void unsubscribe_transponder(TransponderHandle handle);

        



    /**
     * @brief Poll for 'AdsbVehicle' (blocking).
     *
     * @return One AdsbVehicle update.
     */
    AdsbVehicle transponder() const;




    /**
     * @brief Set rate to 'transponder' updates.
     *
     * This function is non-blocking. See 'set_rate_transponder' for the blocking counterpart.
     */
    void set_rate_transponder_async(double rate_hz, const ResultCallback callback);



    /**
     * @brief Set rate to 'transponder' updates.
     *
     * This function is blocking. See 'set_rate_transponder_async' for the non-blocking counterpart.
     *
     
     * @return Result of request.
     
     */
    Result set_rate_transponder(double rate_hz) const;




    /**
     * @brief Copy constructor.
     */
    Transponder(const Transponder& other);

    /**
     * @brief Equality operator (object is not copyable).
     */
    const Transponder& operator=(const Transponder&) = delete;

private:
    /** @private Underlying implementation, set at instantiation */
    std::unique_ptr<TransponderImpl> _impl;
};

} // namespace mavsdk