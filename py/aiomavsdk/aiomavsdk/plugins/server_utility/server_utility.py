# ruff: noqa: F401
# WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
# Edits need to be made to the proto files
# (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/server_utility/server_utility.proto)
"""
Utility for onboard MAVSDK instances for common "server" tasks.
"""

import asyncio
from typing import AsyncGenerator
from pymavsdk.plugins.server_utility import (
    ServerUtility,
    ServerUtilityResult,
    StatusTextType,
)


class ServerUtilityError(Exception):
    """Raised when a ServerUtility operation fails."""

    def __init__(self, result, origin, *params):
        self._result = result
        self._origin = origin
        self._params = params

    def __str__(self):
        return f"{self._result}: '{self._result.name}'; origin: {self._origin}; params: {self._params}"


class ServerUtilityAsync:
    """
    Utility for onboard MAVSDK instances for common "server" tasks.

    Async wrapper around :class:`ServerUtility` that mirrors the gRPC-based
    asyncio API while using the ctypes-based C library directly.
    """

    def __init__(self, system):
        self._subscription_handles: dict = {}
        self._plugin = ServerUtility(system._system)

    async def send_status_text(self, type, text):
        """
        Sends a statustext.

        Parameters
        ----------
        type : <protoc_gen_mavsdk.name_parser.NameParser object at 0x107ccffb0>
        text : std::string
        Raises
        ------
        ServerUtilityError
            If the request fails. The error contains the reason for the failure.
        """
        loop = asyncio.get_running_loop()
        return await loop.run_in_executor(
            None, lambda: self._plugin.send_status_text(type, text)
        )

    def destroy(self):
        self._subscription_handles.clear()
        self._plugin.destroy()

    def __del__(self):
        self.destroy()
