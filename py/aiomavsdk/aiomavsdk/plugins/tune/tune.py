# ruff: noqa: F401
# WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
# Edits need to be made to the proto files
# (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/tune/tune.proto)
"""
Enable creating and sending a tune to be played on the system.
"""

import asyncio
from typing import AsyncGenerator
from pymavsdk.plugins.tune import (
    Tune,
    TuneResult,
    SongElement,
    TuneDescription,
)


class TuneError(Exception):
    """Raised when a Tune operation fails."""

    def __init__(self, result, origin, *params):
        self._result = result
        self._origin = origin
        self._params = params

    def __str__(self):
        return f"{self._result}: '{self._result.name}'; origin: {self._origin}; params: {self._params}"


class TuneAsync:
    """
    Enable creating and sending a tune to be played on the system.

    Async wrapper around :class:`Tune` that mirrors the gRPC-based
    asyncio API while using the ctypes-based C library directly.
    """

    def __init__(self, system):
        self._subscription_handles: dict = {}
        self._plugin = Tune(system._system)

    async def play_tune(self, tune_description):
        """
        Send a tune to be played by the system.

        Parameters
        ----------
        tune_description : <protoc_gen_mavsdk.name_parser.NameParser object at 0x108814c50>
        Raises
        ------
        TuneError
            If the request fails. The error contains the reason for the failure.
        """
        loop = asyncio.get_running_loop()
        return await loop.run_in_executor(
            None, lambda: self._plugin.play_tune(tune_description)
        )

    def destroy(self):
        self._subscription_handles.clear()
        self._plugin.destroy()

    def __del__(self):
        self.destroy()
