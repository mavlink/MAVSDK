# WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
# Edits need to be made to the proto files
# (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/{{ plugin_name.lower_snake_case }}/{{ plugin_name.lower_snake_case }}.proto)
"""
{{ class_description }}
"""
from .{{ plugin_name.lower_snake_case }} import {{ plugin_name.upper_camel_case }}Async

from mavsdk.plugins.{{ plugin_name.lower_snake_case }} import (
    {{ plugin_name.upper_camel_case }},
    {% for enum in enums %}
    {{ enum.name.upper_camel_case }},
    {% endfor %}
    {{ plugin_name.upper_camel_case }}Result,
    {% for struct in structs %}
      {% if not struct.name.upper_camel_case.endswith('Result') %}
    {{ struct.name.upper_camel_case }},
      {% endif %}
    {% endfor %}
)

__all__ = [
    '{{ plugin_name.upper_camel_case }}Async',
    '{{ plugin_name.upper_camel_case }}',
    {% for enum in enums %}
    '{{ enum.name.upper_camel_case }}',
    {% endfor %}
    '{{ plugin_name.upper_camel_case }}Result',
    {% for struct in structs %}
      {% if not struct.name.upper_camel_case.endswith('Result') %}
    '{{ struct.name.upper_camel_case }}',
      {% endif %}
    {% endfor %}
]
