# WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
# Edits need to be made to the proto files
# (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/{{ plugin_name.lower_snake_case }}/{{ plugin_name.lower_snake_case }}.proto)

"""
{{ class_description }}
"""

# Import main plugin class
from .{{ plugin_name.lower_snake_case }} import {{ plugin_name.upper_camel_case }}

# Import enums
{% for enum in enums %}
from .{{ plugin_name.lower_snake_case }} import {{ enum.name.upper_camel_case }}
{% endfor %}

# Import result enum
{% set result_enum = plugin_name.upper_camel_case + "Result" %}
from .{{ plugin_name.lower_snake_case }} import {{ result_enum }}

# Import data structures
{% for struct in structs %}
  {% if not struct.name.upper_camel_case.endswith('Result') %}
from .{{ plugin_name.lower_snake_case }} import {{ struct.name.upper_camel_case }}
  {% endif %}
{% endfor %}

# Define __all__ for explicit exports
__all__ = [
    '{{ plugin_name.upper_camel_case }}',
    {% for enum in enums %}
    '{{ enum.name.upper_camel_case }}',
    {% endfor %}
    '{{ result_enum }}',
    {% for struct in structs %}
      {% if not struct.name.upper_camel_case.endswith('Result') %}
    '{{ struct.name.upper_camel_case }}',
      {% endif %}
    {% endfor %}
]
