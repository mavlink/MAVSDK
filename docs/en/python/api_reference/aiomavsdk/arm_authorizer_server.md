# Arm Authorizer Server

Use arm authorization.

## Structs

### `ArmAuthorizerServerAsync`

Use arm authorization.

Async wrapper around :class:`ArmAuthorizerServer` that mirrors the gRPC-based
asyncio API while using the ctypes-based C library directly.

**Fields:**

- `server_component`
