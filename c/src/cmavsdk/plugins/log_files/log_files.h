// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/log_files/log_files.proto)

#ifndef CMAVSDK_LOG_FILES_H
#define CMAVSDK_LOG_FILES_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Allow to download log files from the vehicle after a flight is complete.
 * For log streaming during flight check the logging plugin.
 */

// ===== Forward Declarations =====
typedef void* mavsdk_system_t;

// ===== Opaque Handles =====
typedef void* mavsdk_log_files_t;
typedef void* mavsdk_log_files_download_log_file_handle_t;

// ===== Enums =====

// ===== Structs =====
/**
 * @brief Progress data coming when downloading a log file.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_log_files_progress_data_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Progress from 0 to 1 */
    float progress;
} mavsdk_log_files_progress_data_t;

/**
 * @brief Destroy a progress_data struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_log_files_progress_data_destroy(
    mavsdk_log_files_progress_data_t* target);

/**
 * @brief Destroy an array of progress_data structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_log_files_progress_data_array_destroy(
    mavsdk_log_files_progress_data_t** array,
    size_t size);

/**
 * @brief Log file entry type.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_log_files_entry_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  ID of the log file, to specify a file to be downloaded */
    uint32_t id;
    /**  Date of the log file in UTC in ISO 8601 format "yyyy-mm-ddThh:mm:ssZ" */
    char* date;
    /**  Size of file in bytes */
    uint32_t size_bytes;
} mavsdk_log_files_entry_t;

/**
 * @brief Destroy a entry struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_log_files_entry_destroy(
    mavsdk_log_files_entry_t* target);

/**
 * @brief Destroy an array of entry structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_log_files_entry_array_destroy(
    mavsdk_log_files_entry_t** array,
    size_t size);

/**
 * @brief Possible results returned for calibration commands
 */
typedef enum {
    /**  Unknown result. */
    MAVSDK_LOG_FILES_RESULT_UNKNOWN = 0,
    /**  Request succeeded. */
    MAVSDK_LOG_FILES_RESULT_SUCCESS = 1,
    /**  Progress update. */
    MAVSDK_LOG_FILES_RESULT_NEXT = 2,
    /**  No log files found. */
    MAVSDK_LOG_FILES_RESULT_NO_LOGFILES = 3,
    /**  A timeout happened. */
    MAVSDK_LOG_FILES_RESULT_TIMEOUT = 4,
    /**  Invalid argument. */
    MAVSDK_LOG_FILES_RESULT_INVALID_ARGUMENT = 5,
    /**  File open failed. */
    MAVSDK_LOG_FILES_RESULT_FILE_OPEN_FAILED = 6,
    /**  No system is connected. */
    MAVSDK_LOG_FILES_RESULT_NO_SYSTEM = 7,
} mavsdk_log_files_result_t;



// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_log_files_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====
typedef void (*mavsdk_log_files_get_entries_callback_t)(const mavsdk_log_files_result_t result, const mavsdk_log_files_entry_t* entries, size_t entries_size, void* user_data);
typedef void (*mavsdk_log_files_download_log_file_callback_t)(const mavsdk_log_files_result_t result, const mavsdk_log_files_progress_data_t progress, void* user_data);

// ===== LogFiles Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_log_files_t mavsdk_log_files_create(mavsdk_system_t system);
CMAVSDK_EXPORT void mavsdk_log_files_destroy(mavsdk_log_files_t log_files);

// ===== Methods =====

/**
 * @brief Get List of log files.
 *
 * @param log_files The log_files instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_log_files_get_entries_async(
    mavsdk_log_files_t log_files,
    mavsdk_log_files_get_entries_callback_t callback,
    void* user_data);


/**
 * @brief Get the current get entries (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param get_entries_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_log_files_result_t
mavsdk_log_files_get_entries(
    mavsdk_log_files_t log_files,
    mavsdk_log_files_entry_t** entries_out,
    size_t* entries_size_out);


/**
 * @brief Download log file.
 *
 * @param log_files The log_files instance.
* @param entry  Entry of the log file to download.
 * 
* @param path  Path of where to download log file to.
 * 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_log_files_download_log_file_async(
    mavsdk_log_files_t log_files,
    mavsdk_log_files_entry_t entry,
    char* path,
    mavsdk_log_files_download_log_file_callback_t callback,
    void* user_data);



/**
 * @brief Get the current erase all log files (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param erase_all_log_files_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_log_files_result_t
mavsdk_log_files_erase_all_log_files(
    mavsdk_log_files_t log_files);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_LOG_FILES_H