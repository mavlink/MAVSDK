// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/param/param.proto)

#ifndef CMAVSDK_PARAM_H
#define CMAVSDK_PARAM_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Provide raw access to get and set parameters.
 */

// ===== Forward Declarations =====
typedef struct mavsdk_system_s *mavsdk_system_t;

// ===== Opaque Handles =====
typedef struct mavsdk_param_s *mavsdk_param_t;

// ===== Enums =====
/**
 * @brief Parameter version
 */
typedef enum {
    /**  Original v1 version. */
    MAVSDK_PARAM_PROTOCOL_VERSION_V1 = 0,
    /**  Extended param version. */
    MAVSDK_PARAM_PROTOCOL_VERSION_EXT = 1,
} mavsdk_param_protocol_version_t;


// ===== Structs =====
/**
 * @brief Type for integer parameters.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_param_int_param_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Name of the parameter */
    char* name;
    /**  Value of the parameter */
    int32_t value;
} mavsdk_param_int_param_t;

/**
 * @brief Destroy a int_param struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_param_int_param_destroy(
    mavsdk_param_int_param_t* target);

/**
 * @brief Destroy an array of int_param structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_param_int_param_array_destroy(
    mavsdk_param_int_param_t** array,
    size_t size);

/**
 * @brief Type for float parameters.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_param_float_param_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Name of the parameter */
    char* name;
    /**  Value of the parameter */
    float value;
} mavsdk_param_float_param_t;

/**
 * @brief Destroy a float_param struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_param_float_param_destroy(
    mavsdk_param_float_param_t* target);

/**
 * @brief Destroy an array of float_param structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_param_float_param_array_destroy(
    mavsdk_param_float_param_t** array,
    size_t size);

/**
 * @brief Type for custom parameters
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_param_custom_param_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Name of the parameter */
    char* name;
    /**  Value of the parameter (max len 128 bytes) */
    char* value;
} mavsdk_param_custom_param_t;

/**
 * @brief Destroy a custom_param struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_param_custom_param_destroy(
    mavsdk_param_custom_param_t* target);

/**
 * @brief Destroy an array of custom_param structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_param_custom_param_array_destroy(
    mavsdk_param_custom_param_t** array,
    size_t size);

/**
 * @brief Type collecting all integer, float, and custom parameters.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_param_all_params_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Collection of all parameter names and values of type int */
    mavsdk_param_int_param_t* int_params;
    size_t int_params_size;
    /**  Collection of all parameter names and values of type float */
    mavsdk_param_float_param_t* float_params;
    size_t float_params_size;
    /**  Collection of all parameter names and values of type custom */
    mavsdk_param_custom_param_t* custom_params;
    size_t custom_params_size;
} mavsdk_param_all_params_t;

/**
 * @brief Destroy a all_params struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_param_all_params_destroy(
    mavsdk_param_all_params_t* target);

/**
 * @brief Destroy an array of all_params structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_param_all_params_array_destroy(
    mavsdk_param_all_params_t** array,
    size_t size);

/**
 * @brief Possible results returned for param requests.
 */
typedef enum {
    /**  Unknown result. */
    MAVSDK_PARAM_RESULT_UNKNOWN = 0,
    /**  Request succeeded. */
    MAVSDK_PARAM_RESULT_SUCCESS = 1,
    /**  Request timed out. */
    MAVSDK_PARAM_RESULT_TIMEOUT = 2,
    /**  Connection error. */
    MAVSDK_PARAM_RESULT_CONNECTION_ERROR = 3,
    /**  Wrong type. */
    MAVSDK_PARAM_RESULT_WRONG_TYPE = 4,
    /**  Parameter name too long (> 16). */
    MAVSDK_PARAM_RESULT_PARAM_NAME_TOO_LONG = 5,
    /**  No system connected. */
    MAVSDK_PARAM_RESULT_NO_SYSTEM = 6,
    /**  Param value too long (> 128). */
    MAVSDK_PARAM_RESULT_PARAM_VALUE_TOO_LONG = 7,
    /**  Operation failed.. */
    MAVSDK_PARAM_RESULT_FAILED = 8,
    /**  Parameter does not exist. */
    MAVSDK_PARAM_RESULT_DOES_NOT_EXIST = 9,
    /**  Parameter value does not fit within accepted range. */
    MAVSDK_PARAM_RESULT_VALUE_OUT_OF_RANGE = 10,
    /**  Caller is not permitted to set the value of this parameter. */
    MAVSDK_PARAM_RESULT_PERMISSION_DENIED = 11,
    /**  Unknown component specified. */
    MAVSDK_PARAM_RESULT_COMPONENT_NOT_FOUND = 12,
    /**  Parameter is read-only. */
    MAVSDK_PARAM_RESULT_READ_ONLY = 13,
    /**  Parameter data type is not supported by flight stack. */
    MAVSDK_PARAM_RESULT_TYPE_UNSUPPORTED = 14,
    /**  Parameter type does not match expected type. */
    MAVSDK_PARAM_RESULT_TYPE_MISMATCH = 15,
    /**  Parameter exists but reading failed. */
    MAVSDK_PARAM_RESULT_READ_FAIL = 16,
} mavsdk_param_result_t;



// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_param_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====

// ===== Param Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_param_t mavsdk_param_create(mavsdk_system_t system);
CMAVSDK_EXPORT void mavsdk_param_destroy(mavsdk_param_t param);

// ===== Methods =====

/**
 * @brief Get the current get param int (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param get_param_int_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_get_param_int(
    mavsdk_param_t param,
    char* name,
    int32_t* value_out);


/**
 * @brief Get the current set param int (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_param_int_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_set_param_int(
    mavsdk_param_t param,
    char* name,
    int32_t value);


/**
 * @brief Get the current get param float (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param get_param_float_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_get_param_float(
    mavsdk_param_t param,
    char* name,
    float* value_out);


/**
 * @brief Get the current set param float (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_param_float_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_set_param_float(
    mavsdk_param_t param,
    char* name,
    float value);


/**
 * @brief Get the current get param custom (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param get_param_custom_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_get_param_custom(
    mavsdk_param_t param,
    char* name,
    char** value_out);


/**
 * @brief Get the current set param custom (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_param_custom_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_set_param_custom(
    mavsdk_param_t param,
    char* name,
    char* value);


/**
 * @brief Get the current get all params (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param get_all_params_out Pointer to store the result.
 */
CMAVSDK_EXPORT
void
mavsdk_param_get_all_params(
    mavsdk_param_t param,
    mavsdk_param_all_params_t* params_out);


/**
 * @brief Get the current select component (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param select_component_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_param_result_t
mavsdk_param_select_component(
    mavsdk_param_t param,
    int32_t component_id,
    mavsdk_param_protocol_version_t protocol_version);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_PARAM_H