// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/offboard/offboard.proto)

#ifndef CMAVSDK_OFFBOARD_H
#define CMAVSDK_OFFBOARD_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Control a drone with position, velocity, attitude or motor commands.
 *
 * The module is called offboard because the commands can be sent from external sources
 * as opposed to onboard control right inside the autopilot "board".
 *
 * Client code must specify a setpoint before starting offboard mode.
 * Mavsdk automatically sends setpoints at 20Hz (PX4 Offboard mode requires that setpoints
 * are minimally sent at 2Hz).
 */

// ===== Forward Declarations =====
typedef struct mavsdk_system_s *mavsdk_system_t;

// ===== Opaque Handles =====
typedef struct mavsdk_offboard_s *mavsdk_offboard_t;

// ===== Enums =====

// ===== Structs =====
/**
 * @brief Type for attitude body angles in NED reference frame (roll, pitch, yaw and thrust)
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_attitude_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Roll angle (in degrees, positive is right side down) */
    float roll_deg;
    /**  Pitch angle (in degrees, positive is nose up) */
    float pitch_deg;
    /**  Yaw angle (in degrees, positive is move nose to the right) */
    float yaw_deg;
    /**  Thrust (range: 0 to 1) */
    float thrust_value;
} mavsdk_offboard_attitude_t;

/**
 * @brief Destroy a attitude struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_attitude_destroy(
    mavsdk_offboard_attitude_t* target);

/**
 * @brief Destroy an array of attitude structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_attitude_array_destroy(
    mavsdk_offboard_attitude_t** array,
    size_t size);

/**
 * @brief Eight controls that will be given to the group. Each control is a normalized
 *  (-1..+1) command value, which will be mapped and scaled through the mixer.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_actuator_control_group_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Controls in the group */
    float* controls;
    size_t controls_size;
} mavsdk_offboard_actuator_control_group_t;

/**
 * @brief Destroy a actuator_control_group struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_actuator_control_group_destroy(
    mavsdk_offboard_actuator_control_group_t* target);

/**
 * @brief Destroy an array of actuator_control_group structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_actuator_control_group_array_destroy(
    mavsdk_offboard_actuator_control_group_t** array,
    size_t size);

/**
 * @brief Type for actuator control.
 * 
 *  Control members should be normed to -1..+1 where 0 is neutral position.
 *  Throttle for single rotation direction motors is 0..1, negative range for reverse direction.
 * 
 *  One group support eight controls.
 * 
 *  Up to 16 actuator controls can be set. To ignore an output group, set all it controls to NaN.
 *  If one or more controls in group is not NaN, then all NaN controls will sent as zero.
 *  The first 8 actuator controls internally map to control group 0, the latter 8 actuator
 *  controls map to control group 1. Depending on what controls are set (instead of NaN) 1 or 2
 *  MAVLink messages are actually sent.
 * 
 *  In PX4 v1.9.0 Only first four Control Groups are supported
 *  (https://github.com/PX4/Firmware/blob/v1.9.0/src/modules/mavlink/mavlink_receiver.cpp#L980).
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_actuator_control_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Control groups. */
    mavsdk_offboard_actuator_control_group_t* groups;
    size_t groups_size;
} mavsdk_offboard_actuator_control_t;

/**
 * @brief Destroy a actuator_control struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_actuator_control_destroy(
    mavsdk_offboard_actuator_control_t* target);

/**
 * @brief Destroy an array of actuator_control structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_actuator_control_array_destroy(
    mavsdk_offboard_actuator_control_t** array,
    size_t size);

/**
 * @brief Type for attitude rate commands in body coordinates (roll, pitch, yaw angular rate and thrust)
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_attitude_rate_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Roll angular rate (in degrees/second, positive for clock-wise looking from front) */
    float roll_deg_s;
    /**  Pitch angular rate (in degrees/second, positive for head/front moving up) */
    float pitch_deg_s;
    /**  Yaw angular rate (in degrees/second, positive for clock-wise looking from above) */
    float yaw_deg_s;
    /**  Thrust (range: 0 to 1) */
    float thrust_value;
} mavsdk_offboard_attitude_rate_t;

/**
 * @brief Destroy a attitude_rate struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_attitude_rate_destroy(
    mavsdk_offboard_attitude_rate_t* target);

/**
 * @brief Destroy an array of attitude_rate structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_attitude_rate_array_destroy(
    mavsdk_offboard_attitude_rate_t** array,
    size_t size);

/**
 * @brief Type for position commands in NED (North East Down) coordinates and yaw.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_position_ned_yaw_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Position North (in metres) */
    float north_m;
    /**  Position East (in metres) */
    float east_m;
    /**  Position Down (in metres) */
    float down_m;
    /**  Yaw in degrees (0 North, positive is clock-wise looking from above) */
    float yaw_deg;
} mavsdk_offboard_position_ned_yaw_t;

/**
 * @brief Destroy a position_ned_yaw struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_position_ned_yaw_destroy(
    mavsdk_offboard_position_ned_yaw_t* target);

/**
 * @brief Destroy an array of position_ned_yaw structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_position_ned_yaw_array_destroy(
    mavsdk_offboard_position_ned_yaw_t** array,
    size_t size);

/**
 * @brief Possible altitude options
 */
typedef enum {
    /**  Altitude relative to the Home position. */
    MAVSDK_OFFBOARD_POSITION_GLOBAL_YAW_ALTITUDE_TYPE_REL_HOME = 0,
    /**  Altitude above mean sea level (AMSL). */
    MAVSDK_OFFBOARD_POSITION_GLOBAL_YAW_ALTITUDE_TYPE_AMSL = 1,
    /**  Altitude above ground level (AGL). */
    MAVSDK_OFFBOARD_POSITION_GLOBAL_YAW_ALTITUDE_TYPE_AGL = 2,
} mavsdk_offboard_position_global_yaw_altitude_type_t;

/**
 * @brief Type for position commands in Global (Latitude, Longitude, Altitude) coordinates and yaw.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_position_global_yaw_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Latitude (in degrees) */
    double lat_deg;
    /**  Longitude (in degrees) */
    double lon_deg;
    /**  altitude (in metres) */
    float alt_m;
    /**  Yaw in degrees (0 North, positive is clock-wise looking from above) */
    float yaw_deg;
    /**  altitude type for this position */
    mavsdk_offboard_position_global_yaw_altitude_type_t altitude_type;
} mavsdk_offboard_position_global_yaw_t;

/**
 * @brief Destroy a position_global_yaw struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_position_global_yaw_destroy(
    mavsdk_offboard_position_global_yaw_t* target);

/**
 * @brief Destroy an array of position_global_yaw structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_position_global_yaw_array_destroy(
    mavsdk_offboard_position_global_yaw_t** array,
    size_t size);

/**
 * @brief Type for velocity commands in body coordinates.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_velocity_body_yawspeed_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Velocity forward (in metres/second) */
    float forward_m_s;
    /**  Velocity right (in metres/second) */
    float right_m_s;
    /**  Velocity down (in metres/second) */
    float down_m_s;
    /**  Yaw angular rate (in degrees/second, positive for clock-wise looking from above) */
    float yawspeed_deg_s;
} mavsdk_offboard_velocity_body_yawspeed_t;

/**
 * @brief Destroy a velocity_body_yawspeed struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_velocity_body_yawspeed_destroy(
    mavsdk_offboard_velocity_body_yawspeed_t* target);

/**
 * @brief Destroy an array of velocity_body_yawspeed structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_velocity_body_yawspeed_array_destroy(
    mavsdk_offboard_velocity_body_yawspeed_t** array,
    size_t size);

/**
 * @brief Type for velocity commands in NED (North East Down) coordinates and yaw.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_velocity_ned_yaw_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Velocity North (in metres/second) */
    float north_m_s;
    /**  Velocity East (in metres/second) */
    float east_m_s;
    /**  Velocity Down (in metres/second) */
    float down_m_s;
    /**  Yaw in degrees (0 North, positive is clock-wise looking from above) */
    float yaw_deg;
} mavsdk_offboard_velocity_ned_yaw_t;

/**
 * @brief Destroy a velocity_ned_yaw struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_velocity_ned_yaw_destroy(
    mavsdk_offboard_velocity_ned_yaw_t* target);

/**
 * @brief Destroy an array of velocity_ned_yaw structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_velocity_ned_yaw_array_destroy(
    mavsdk_offboard_velocity_ned_yaw_t** array,
    size_t size);

/**
 * @brief Type for acceleration commands in NED (North East Down) coordinates.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_offboard_acceleration_ned_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Acceleration North (in metres/second^2) */
    float north_m_s2;
    /**  Acceleration East (in metres/second^2) */
    float east_m_s2;
    /**  Acceleration Down (in metres/second^2) */
    float down_m_s2;
} mavsdk_offboard_acceleration_ned_t;

/**
 * @brief Destroy a acceleration_ned struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_offboard_acceleration_ned_destroy(
    mavsdk_offboard_acceleration_ned_t* target);

/**
 * @brief Destroy an array of acceleration_ned structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_offboard_acceleration_ned_array_destroy(
    mavsdk_offboard_acceleration_ned_t** array,
    size_t size);

/**
 * @brief Possible results returned for offboard requests
 */
typedef enum {
    /**  Unknown result. */
    MAVSDK_OFFBOARD_RESULT_UNKNOWN = 0,
    /**  Request succeeded. */
    MAVSDK_OFFBOARD_RESULT_SUCCESS = 1,
    /**  No system is connected. */
    MAVSDK_OFFBOARD_RESULT_NO_SYSTEM = 2,
    /**  Connection error. */
    MAVSDK_OFFBOARD_RESULT_CONNECTION_ERROR = 3,
    /**  Vehicle is busy. */
    MAVSDK_OFFBOARD_RESULT_BUSY = 4,
    /**  Command denied. */
    MAVSDK_OFFBOARD_RESULT_COMMAND_DENIED = 5,
    /**  Request timed out. */
    MAVSDK_OFFBOARD_RESULT_TIMEOUT = 6,
    /**  Cannot start without setpoint set. */
    MAVSDK_OFFBOARD_RESULT_NO_SETPOINT_SET = 7,
    /**  Request failed. */
    MAVSDK_OFFBOARD_RESULT_FAILED = 8,
} mavsdk_offboard_result_t;



// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_offboard_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====
typedef void (*mavsdk_offboard_start_callback_t)(const mavsdk_offboard_result_t result, void* user_data);
typedef void (*mavsdk_offboard_stop_callback_t)(const mavsdk_offboard_result_t result, void* user_data);

// ===== Offboard Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_offboard_t mavsdk_offboard_create(mavsdk_system_t system);
CMAVSDK_EXPORT void mavsdk_offboard_destroy(mavsdk_offboard_t offboard);

// ===== Methods =====

/**
 * @brief Start offboard control.
 *
 * @param offboard The offboard instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_offboard_start_async(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_start_callback_t callback,
    void* user_data);


/**
 * @brief Get the current start (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param start_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_start(
    mavsdk_offboard_t offboard);


/**
 * @brief Stop offboard control.
 * 
 *  The vehicle will be put into Hold mode: https://docs.px4.io/en/flight_modes/hold.html
 *
 * @param offboard The offboard instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_offboard_stop_async(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_stop_callback_t callback,
    void* user_data);


/**
 * @brief Get the current stop (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param stop_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_stop(
    mavsdk_offboard_t offboard);


/**
 * @brief Get the current is active (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param is_active_out Pointer to store the result.
 */
CMAVSDK_EXPORT
void
mavsdk_offboard_is_active(
    mavsdk_offboard_t offboard,
    bool* is_active_out);


/**
 * @brief Get the current set attitude (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_attitude_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_attitude(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_attitude_t attitude);


/**
 * @brief Get the current set actuator control (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_actuator_control_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_actuator_control(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_actuator_control_t actuator_control);


/**
 * @brief Get the current set attitude rate (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_attitude_rate_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_attitude_rate(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_attitude_rate_t attitude_rate);


/**
 * @brief Get the current set position ned (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_position_ned_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_position_ned(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_position_ned_yaw_t position_ned_yaw);


/**
 * @brief Get the current set position global (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_position_global_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_position_global(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_position_global_yaw_t position_global_yaw);


/**
 * @brief Get the current set velocity body (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_velocity_body_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_velocity_body(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_velocity_body_yawspeed_t velocity_body_yawspeed);


/**
 * @brief Get the current set velocity ned (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_velocity_ned_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_velocity_ned(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_velocity_ned_yaw_t velocity_ned_yaw);


/**
 * @brief Get the current set position velocity ned (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_position_velocity_ned_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_position_velocity_ned(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_position_ned_yaw_t position_ned_yaw,
    mavsdk_offboard_velocity_ned_yaw_t velocity_ned_yaw);


/**
 * @brief Get the current set position velocity acceleration ned (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_position_velocity_acceleration_ned_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_position_velocity_acceleration_ned(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_position_ned_yaw_t position_ned_yaw,
    mavsdk_offboard_velocity_ned_yaw_t velocity_ned_yaw,
    mavsdk_offboard_acceleration_ned_t acceleration_ned);


/**
 * @brief Get the current set acceleration ned (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_acceleration_ned_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_offboard_result_t
mavsdk_offboard_set_acceleration_ned(
    mavsdk_offboard_t offboard,
    mavsdk_offboard_acceleration_ned_t acceleration_ned);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_OFFBOARD_H