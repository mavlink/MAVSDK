// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/mission_raw/mission_raw.proto)

#ifndef CMAVSDK_MISSION_RAW_H
#define CMAVSDK_MISSION_RAW_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Enable raw missions as exposed by MAVLink.
 */

// ===== Forward Declarations =====
typedef void* mavsdk_system_t;

// ===== Opaque Handles =====
typedef void* mavsdk_mission_raw_t;
typedef void* mavsdk_mission_raw_mission_progress_handle_t;
typedef void* mavsdk_mission_raw_mission_changed_handle_t;

// ===== Enums =====

// ===== Structs =====
/**
 * @brief Mission progress type.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_mission_raw_mission_progress_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Current mission item index (0-based), if equal to total, the mission is finished */
    int32_t current;
    /**  Total number of mission items */
    int32_t total;
} mavsdk_mission_raw_mission_progress_t;

/**
 * @brief Destroy a mission_progress struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_mission_progress_destroy(
    mavsdk_mission_raw_mission_progress_t* target);

/**
 * @brief Destroy an array of mission_progress structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_mission_progress_array_destroy(
    mavsdk_mission_raw_mission_progress_t** array,
    size_t size);

/**
 * @brief Mission item exactly identical to MAVLink MISSION_ITEM_INT.
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_mission_raw_mission_item_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Sequence (uint16_t) */
    uint32_t seq;
    /**  The coordinate system of the waypoint (actually uint8_t) */
    uint32_t frame;
    /**  The scheduled action for the waypoint (actually uint16_t) */
    uint32_t command;
    /**  false:0, true:1 (actually uint8_t) */
    uint32_t current;
    /**  Autocontinue to next waypoint (actually uint8_t) */
    uint32_t autocontinue;
    /**  PARAM1, see MAV_CMD enum */
    float param1;
    /**  PARAM2, see MAV_CMD enum */
    float param2;
    /**  PARAM3, see MAV_CMD enum */
    float param3;
    /**  PARAM4, see MAV_CMD enum */
    float param4;
    /**  PARAM5 / local: x position in meters * 1e4, global: latitude in degrees * 10^7 */
    int32_t x;
    /**  PARAM6 / y position: local: x position in meters * 1e4, global: longitude in degrees *10^7 */
    int32_t y;
    /**  PARAM7 / local: Z coordinate, global: altitude (relative or absolute, depending on frame) */
    float z;
    /**  Mission type (actually uint8_t) */
    uint32_t mission_type;
} mavsdk_mission_raw_mission_item_t;

/**
 * @brief Destroy a mission_item struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_mission_item_destroy(
    mavsdk_mission_raw_mission_item_t* target);

/**
 * @brief Destroy an array of mission_item structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_mission_item_array_destroy(
    mavsdk_mission_raw_mission_item_t** array,
    size_t size);

/**
 * @brief Mission import data
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_mission_raw_mission_import_data_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  Mission items */
    mavsdk_mission_raw_mission_item_t* mission_items;
    size_t mission_items_size;
    /**  Geofence items */
    mavsdk_mission_raw_mission_item_t* geofence_items;
    size_t geofence_items_size;
    /**  Rally items */
    mavsdk_mission_raw_mission_item_t* rally_items;
    size_t rally_items_size;
} mavsdk_mission_raw_mission_import_data_t;

/**
 * @brief Destroy a mission_import_data struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_mission_import_data_destroy(
    mavsdk_mission_raw_mission_import_data_t* target);

/**
 * @brief Destroy an array of mission_import_data structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_mission_import_data_array_destroy(
    mavsdk_mission_raw_mission_import_data_t** array,
    size_t size);

/**
 * @brief Possible results returned for action requests.
 */
typedef enum {
    /**  Unknown result. */
    MAVSDK_MISSION_RAW_RESULT_UNKNOWN = 0,
    /**  Request succeeded. */
    MAVSDK_MISSION_RAW_RESULT_SUCCESS = 1,
    /**  Error. */
    MAVSDK_MISSION_RAW_RESULT_ERROR = 2,
    /**  Too many mission items in the mission. */
    MAVSDK_MISSION_RAW_RESULT_TOO_MANY_MISSION_ITEMS = 3,
    /**  Vehicle is busy. */
    MAVSDK_MISSION_RAW_RESULT_BUSY = 4,
    /**  Request timed out. */
    MAVSDK_MISSION_RAW_RESULT_TIMEOUT = 5,
    /**  Invalid argument. */
    MAVSDK_MISSION_RAW_RESULT_INVALID_ARGUMENT = 6,
    /**  Mission downloaded from the system is not supported. */
    MAVSDK_MISSION_RAW_RESULT_UNSUPPORTED = 7,
    /**  No mission available on the system. */
    MAVSDK_MISSION_RAW_RESULT_NO_MISSION_AVAILABLE = 8,
    /**  Mission transfer (upload or download) has been cancelled. */
    MAVSDK_MISSION_RAW_RESULT_TRANSFER_CANCELLED = 9,
    /**  Failed to open the QGroundControl plan. */
    MAVSDK_MISSION_RAW_RESULT_FAILED_TO_OPEN_QGC_PLAN = 10,
    /**  Failed to parse the QGroundControl plan. */
    MAVSDK_MISSION_RAW_RESULT_FAILED_TO_PARSE_QGC_PLAN = 11,
    /**  No system connected. */
    MAVSDK_MISSION_RAW_RESULT_NO_SYSTEM = 12,
    /**  Request denied. */
    MAVSDK_MISSION_RAW_RESULT_DENIED = 13,
    /**  Mission type is not consistent. */
    MAVSDK_MISSION_RAW_RESULT_MISSION_TYPE_NOT_CONSISTENT = 14,
    /**  The mission item sequences are not increasing correctly. */
    MAVSDK_MISSION_RAW_RESULT_INVALID_SEQUENCE = 15,
    /**  The current item is not set correctly. */
    MAVSDK_MISSION_RAW_RESULT_CURRENT_INVALID = 16,
    /**  There was a protocol error. */
    MAVSDK_MISSION_RAW_RESULT_PROTOCOL_ERROR = 17,
    /**  The system does not support the MISSION_INT protocol. */
    MAVSDK_MISSION_RAW_RESULT_INT_MESSAGES_NOT_SUPPORTED = 18,
    /**  Failed to open the Mission Planner plan. */
    MAVSDK_MISSION_RAW_RESULT_FAILED_TO_OPEN_MISSION_PLANNER_PLAN = 19,
    /**  Failed to parse the Mission Planner plan. */
    MAVSDK_MISSION_RAW_RESULT_FAILED_TO_PARSE_MISSION_PLANNER_PLAN = 20,
} mavsdk_mission_raw_result_t;



// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====
typedef void (*mavsdk_mission_raw_upload_mission_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_upload_geofence_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_upload_rally_points_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_download_mission_callback_t)(const mavsdk_mission_raw_result_t result, const mavsdk_mission_raw_mission_item_t* mission_items, size_t mission_items_size, void* user_data);
typedef void (*mavsdk_mission_raw_download_geofence_callback_t)(const mavsdk_mission_raw_result_t result, const mavsdk_mission_raw_mission_item_t* geofence_items, size_t geofence_items_size, void* user_data);
typedef void (*mavsdk_mission_raw_download_rallypoints_callback_t)(const mavsdk_mission_raw_result_t result, const mavsdk_mission_raw_mission_item_t* rallypoint_items, size_t rallypoint_items_size, void* user_data);
typedef void (*mavsdk_mission_raw_start_mission_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_pause_mission_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_clear_mission_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_set_current_mission_item_callback_t)(const mavsdk_mission_raw_result_t result, void* user_data);
typedef void (*mavsdk_mission_raw_mission_progress_callback_t)(const mavsdk_mission_raw_mission_progress_t mission_progress, void* user_data);
typedef void (*mavsdk_mission_raw_mission_changed_callback_t)(const bool mission_changed, void* user_data);

// ===== MissionRaw Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_mission_raw_t mavsdk_mission_raw_create(mavsdk_system_t system);
CMAVSDK_EXPORT void mavsdk_mission_raw_destroy(mavsdk_mission_raw_t mission_raw);

// ===== Methods =====

/**
 * @brief Upload a list of raw mission items to the system.
 * 
 *  The raw mission items are uploaded to a drone. Once uploaded the mission
 *  can be started and executed even if the connection is lost.
 *
 * @param mission_raw The mission_raw instance.
* @param mission_items  The mission items
 * 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_upload_mission_async(
    mavsdk_mission_raw_t mission_raw,
    const mavsdk_mission_raw_mission_item_t* mission_items,
    size_t mission_items_size,
    mavsdk_mission_raw_upload_mission_callback_t callback,
    void* user_data);


/**
 * @brief Get the current upload mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param upload_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_upload_mission(
    mavsdk_mission_raw_t mission_raw,
    const mavsdk_mission_raw_mission_item_t* mission_items,
    size_t mission_items_size);


/**
 * @brief Upload a list of geofence items to the system.
 *
 * @param mission_raw The mission_raw instance.
* @param mission_items  The mission items
 * 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_upload_geofence_async(
    mavsdk_mission_raw_t mission_raw,
    const mavsdk_mission_raw_mission_item_t* mission_items,
    size_t mission_items_size,
    mavsdk_mission_raw_upload_geofence_callback_t callback,
    void* user_data);


/**
 * @brief Get the current upload geofence (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param upload_geofence_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_upload_geofence(
    mavsdk_mission_raw_t mission_raw,
    const mavsdk_mission_raw_mission_item_t* mission_items,
    size_t mission_items_size);


/**
 * @brief Upload a list of rally point items to the system.
 *
 * @param mission_raw The mission_raw instance.
* @param mission_items  The mission items
 * 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_upload_rally_points_async(
    mavsdk_mission_raw_t mission_raw,
    const mavsdk_mission_raw_mission_item_t* mission_items,
    size_t mission_items_size,
    mavsdk_mission_raw_upload_rally_points_callback_t callback,
    void* user_data);


/**
 * @brief Get the current upload rally points (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param upload_rally_points_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_upload_rally_points(
    mavsdk_mission_raw_t mission_raw,
    const mavsdk_mission_raw_mission_item_t* mission_items,
    size_t mission_items_size);


/**
 * @brief Get the current cancel mission upload (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param cancel_mission_upload_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_cancel_mission_upload(
    mavsdk_mission_raw_t mission_raw);


/**
 * @brief Download a list of raw mission items from the system (asynchronous).
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_download_mission_async(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_download_mission_callback_t callback,
    void* user_data);


/**
 * @brief Get the current download mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param download_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_download_mission(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_item_t** mission_items_out,
    size_t* mission_items_size_out);


/**
 * @brief Download a list of raw geofence items from the system (asynchronous).
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_download_geofence_async(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_download_geofence_callback_t callback,
    void* user_data);


/**
 * @brief Get the current download geofence (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param download_geofence_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_download_geofence(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_item_t** geofence_items_out,
    size_t* geofence_items_size_out);


/**
 * @brief Download a list of raw rallypoint items from the system (asynchronous).
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_download_rallypoints_async(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_download_rallypoints_callback_t callback,
    void* user_data);


/**
 * @brief Get the current download rallypoints (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param download_rallypoints_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_download_rallypoints(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_item_t** rallypoint_items_out,
    size_t* rallypoint_items_size_out);


/**
 * @brief Get the current cancel mission download (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param cancel_mission_download_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_cancel_mission_download(
    mavsdk_mission_raw_t mission_raw);


/**
 * @brief Start the mission.
 * 
 *  A mission must be uploaded to the vehicle before this can be called.
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_start_mission_async(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_start_mission_callback_t callback,
    void* user_data);


/**
 * @brief Get the current start mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param start_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_start_mission(
    mavsdk_mission_raw_t mission_raw);


/**
 * @brief Pause the mission.
 * 
 *  Pausing the mission puts the vehicle into
 *  [HOLD mode](https://docs.px4.io/en/flight_modes/hold.html).
 *  A multicopter should just hover at the spot while a fixedwing vehicle should loiter
 *  around the location where it paused.
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_pause_mission_async(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_pause_mission_callback_t callback,
    void* user_data);


/**
 * @brief Get the current pause mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param pause_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_pause_mission(
    mavsdk_mission_raw_t mission_raw);


/**
 * @brief Clear the mission saved on the vehicle.
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_clear_mission_async(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_clear_mission_callback_t callback,
    void* user_data);


/**
 * @brief Get the current clear mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param clear_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_clear_mission(
    mavsdk_mission_raw_t mission_raw);


/**
 * @brief Sets the raw mission item index to go to.
 * 
 *  By setting the current index to 0, the mission is restarted from the beginning. If it is set
 *  to a specific index of a raw mission item, the mission will be set to this item.
 *
 * @param mission_raw The mission_raw instance.
* @param index  Index of the mission item to be set as the next one (0-based)
 * 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_set_current_mission_item_async(
    mavsdk_mission_raw_t mission_raw,
    int32_t index,
    mavsdk_mission_raw_set_current_mission_item_callback_t callback,
    void* user_data);


/**
 * @brief Get the current set current mission item (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_current_mission_item_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_set_current_mission_item(
    mavsdk_mission_raw_t mission_raw,
    int32_t index);


/**
 * @brief Subscribe to mission progress updates.
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 * @return Handle for this subscription. Use mavsdk_mission_raw_unsubscribe_mission_progress() to unsubscribe.
 */
CMAVSDK_EXPORT mavsdk_mission_raw_mission_progress_handle_t mavsdk_mission_raw_subscribe_mission_progress(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_progress_callback_t callback,
    void* user_data);

/**
 * @brief Unsubscribe from 'mission_progress' updates.
 *
 * Stops the subscription and frees resources associated with the handle.
 *
 * @param mission_raw The mission_raw instance.
 * @param handle The subscription handle returned by mavsdk_mission_raw_subscribe_mission_progress().
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_unsubscribe_mission_progress(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_progress_handle_t);

/**
 * @brief Get the current mission progress (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param mission_progress_out Pointer to store the result.
 */
CMAVSDK_EXPORT
void
mavsdk_mission_raw_mission_progress(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_progress_t* mission_progress_out);


/**
 * @brief Subscribes to mission changed.
 * 
 *  This notification can be used to be informed if a ground station has
 *  been uploaded or changed by a ground station or companion computer.
 * 
 *  @param callback Callback to notify about change.
 *
 * @param mission_raw The mission_raw instance.
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 * @return Handle for this subscription. Use mavsdk_mission_raw_unsubscribe_mission_changed() to unsubscribe.
 */
CMAVSDK_EXPORT mavsdk_mission_raw_mission_changed_handle_t mavsdk_mission_raw_subscribe_mission_changed(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_changed_callback_t callback,
    void* user_data);

/**
 * @brief Unsubscribe from 'mission_changed' updates.
 *
 * Stops the subscription and frees resources associated with the handle.
 *
 * @param mission_raw The mission_raw instance.
 * @param handle The subscription handle returned by mavsdk_mission_raw_subscribe_mission_changed().
 */
CMAVSDK_EXPORT void mavsdk_mission_raw_unsubscribe_mission_changed(
    mavsdk_mission_raw_t mission_raw,
    mavsdk_mission_raw_mission_changed_handle_t);


/**
 * @brief Get the current import qgroundcontrol mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param import_qgroundcontrol_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_import_qgroundcontrol_mission(
    mavsdk_mission_raw_t mission_raw,
    char* qgc_plan_path,
    mavsdk_mission_raw_mission_import_data_t* mission_import_data_out);


/**
 * @brief Get the current import qgroundcontrol mission from string (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param import_qgroundcontrol_mission_from_string_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_import_qgroundcontrol_mission_from_string(
    mavsdk_mission_raw_t mission_raw,
    char* qgc_plan,
    mavsdk_mission_raw_mission_import_data_t* mission_import_data_out);


/**
 * @brief Get the current import mission planner mission (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param import_mission_planner_mission_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_import_mission_planner_mission(
    mavsdk_mission_raw_t mission_raw,
    char* mission_planner_path,
    mavsdk_mission_raw_mission_import_data_t* mission_import_data_out);


/**
 * @brief Get the current import mission planner mission from string (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param import_mission_planner_mission_from_string_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_import_mission_planner_mission_from_string(
    mavsdk_mission_raw_t mission_raw,
    char* mission_planner_mission,
    mavsdk_mission_raw_mission_import_data_t* mission_import_data_out);


/**
 * @brief Get the current is mission finished (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param is_mission_finished_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_mission_raw_result_t
mavsdk_mission_raw_is_mission_finished(
    mavsdk_mission_raw_t mission_raw,
    bool* is_finished_out);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_MISSION_RAW_H