// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/component_metadata_server/component_metadata_server.proto)

#ifndef CMAVSDK_COMPONENT_METADATA_SERVER_H
#define CMAVSDK_COMPONENT_METADATA_SERVER_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Provide component metadata json definitions, such as parameters.
 */

// ===== Forward Declarations =====
typedef struct mavsdk_server_component_s *mavsdk_server_component_t;

// ===== Opaque Handles =====
typedef struct mavsdk_component_metadata_server_s *mavsdk_component_metadata_server_t;

// ===== Enums =====
/**
 * @brief The metadata type
 */
typedef enum {
    /**  Parameter metadata. */
    MAVSDK_COMPONENT_METADATA_SERVER_METADATA_TYPE_PARAMETER = 0,
    /**  Event definitions. */
    MAVSDK_COMPONENT_METADATA_SERVER_METADATA_TYPE_EVENTS = 1,
    /**  Actuator definitions. */
    MAVSDK_COMPONENT_METADATA_SERVER_METADATA_TYPE_ACTUATORS = 2,
} mavsdk_component_metadata_server_metadata_type_t;


// ===== Structs =====
/**
 * @brief The metadata type and content
 *
 * @note This struct may contain dynamically allocated memory. Always call
 *       mavsdk_component_metadata_server_metadata_destroy() when done to avoid memory leaks.
 */
typedef struct CMAVSDK_EXPORT {
    /**  The metadata type */
    mavsdk_component_metadata_server_metadata_type_t type;
    /**  The JSON metadata */
    char* json_metadata;
} mavsdk_component_metadata_server_metadata_t;

/**
 * @brief Destroy a metadata struct.
 *
 * Frees all memory allocated by MAVSDK for this struct, including any
 * dynamically allocated arrays or strings. Must be called to avoid memory leaks.
 * Always call this function when done with the struct, even if it currently
 * contains no dynamic allocations.
 *
 * @param target Pointer to the struct to destroy. Can be NULL (no-op).
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_metadata_destroy(
    mavsdk_component_metadata_server_metadata_t* target);

/**
 * @brief Destroy an array of metadata structs.
 *
 * Frees all memory allocated for the array and its elements, including any
 * nested dynamic allocations. Must be called to avoid memory leaks.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 * @param size Number of elements in the array.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_metadata_array_destroy(
    mavsdk_component_metadata_server_metadata_t** array,
    size_t size);


// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_component_metadata_server_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====

// ===== ComponentMetadataServer Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_component_metadata_server_t mavsdk_component_metadata_server_create(mavsdk_server_component_t server_component);
CMAVSDK_EXPORT void mavsdk_component_metadata_server_destroy(mavsdk_component_metadata_server_t component_metadata_server);

// ===== Methods =====

/**
 * @brief Get the current set metadata (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_metadata_out Pointer to store the result.
 */
CMAVSDK_EXPORT
void
mavsdk_component_metadata_server_set_metadata(
    mavsdk_component_metadata_server_t component_metadata_server,
    const mavsdk_component_metadata_server_metadata_t* metadata,
    size_t metadata_size);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_COMPONENT_METADATA_SERVER_H