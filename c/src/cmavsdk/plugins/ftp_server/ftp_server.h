// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/ftp_server/ftp_server.proto)

#ifndef CMAVSDK_FTP_SERVER_H
#define CMAVSDK_FTP_SERVER_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Provide files or directories to transfer.
 */

// ===== Forward Declarations =====
typedef void* mavsdk_server_component_t;

// ===== Opaque Handles =====
typedef void* mavsdk_ftp_server_t;

// ===== Enums =====

// ===== Structs =====
/**
 * @brief Possible results returned for FTP server requests.
 */
typedef enum {
    /**  Unknown result. */
    MAVSDK_FTP_SERVER_RESULT_UNKNOWN = 0,
    /**  Request succeeded. */
    MAVSDK_FTP_SERVER_RESULT_SUCCESS = 1,
    /**  Directory does not exist. */
    MAVSDK_FTP_SERVER_RESULT_DOES_NOT_EXIST = 2,
    /**  Operations in progress. */
    MAVSDK_FTP_SERVER_RESULT_BUSY = 3,
} mavsdk_ftp_server_result_t;



// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_ftp_server_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====

// ===== FtpServer Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_ftp_server_t mavsdk_ftp_server_create(mavsdk_server_component_t server_component);
CMAVSDK_EXPORT void mavsdk_ftp_server_destroy(mavsdk_ftp_server_t ftp_server);

// ===== Methods =====

/**
 * @brief Get the current set root dir (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param set_root_dir_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_ftp_server_result_t
mavsdk_ftp_server_set_root_dir(
    mavsdk_ftp_server_t ftp_server,
    char* path);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_FTP_SERVER_H