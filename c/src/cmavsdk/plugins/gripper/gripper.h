// WARNING: THIS FILE IS AUTOGENERATED! As such, it should not be edited.
// Edits need to be made to the proto files
// (see https://github.com/mavlink/MAVSDK-Proto/blob/main/protos/gripper/gripper.proto)

#ifndef CMAVSDK_GRIPPER_H
#define CMAVSDK_GRIPPER_H

#include <stddef.h>
#include <stdint.h>
#include <stdbool.h>
#include "cmavsdk/mavsdk_export.h"

#ifdef __cplusplus
extern "C" {
#endif

/**
 * @brief Allows users to send gripper actions.
 */

// ===== Forward Declarations =====
typedef struct mavsdk_system_s *mavsdk_system_t;

// ===== Opaque Handles =====
typedef struct mavsdk_gripper_s *mavsdk_gripper_t;

// ===== Enums =====
/**
 * @brief Gripper Actions.
 *
 * Available gripper actions are defined in mavlink under
 * https://mavlink.io/en/messages/common.html#GRIPPER_ACTIONS
 */
typedef enum {
    /**  Open the gripper to release the cargo. */
    MAVSDK_GRIPPER_GRIPPER_ACTION_RELEASE = 0,
    /**  Close the gripper and grab onto cargo. */
    MAVSDK_GRIPPER_GRIPPER_ACTION_GRAB = 1,
} mavsdk_gripper_gripper_action_t;


// ===== Structs =====
/**
 * @brief Possible results returned for gripper action requests.
 */
typedef enum {
    /**  Unknown result. */
    MAVSDK_GRIPPER_RESULT_UNKNOWN = 0,
    /**  Request was successful. */
    MAVSDK_GRIPPER_RESULT_SUCCESS = 1,
    /**  No system is connected. */
    MAVSDK_GRIPPER_RESULT_NO_SYSTEM = 2,
    /**  Temporarily rejected. */
    MAVSDK_GRIPPER_RESULT_BUSY = 3,
    /**  Request timed out. */
    MAVSDK_GRIPPER_RESULT_TIMEOUT = 4,
    /**  Action not supported. */
    MAVSDK_GRIPPER_RESULT_UNSUPPORTED = 5,
    /**  Action failed. */
    MAVSDK_GRIPPER_RESULT_FAILED = 6,
} mavsdk_gripper_result_t;



// ===== Primitive Array Destroy Functions =====
/**
 * @brief Destroy an array of float.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_float_array_destroy(float** array);
/**
 * @brief Destroy an array of double.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_double_array_destroy(double** array);
/**
 * @brief Destroy an array of int32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_int32t_array_destroy(int32_t** array);
/**
 * @brief Destroy an array of uint32_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_uint32t_array_destroy(uint32_t** array);
/**
 * @brief Destroy an array of int64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_int64t_array_destroy(int64_t** array);
/**
 * @brief Destroy an array of uint64_t.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_uint64t_array_destroy(uint64_t** array);
/**
 * @brief Destroy an array of bool.
 *
 * Frees the memory allocated for the array.
 *
 * @param array Pointer to the array pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_bool_array_destroy(bool** array);

/**
 * @brief Destroy a string (char*).
 *
 * Frees the memory allocated for the string.
 *
 * @param str Pointer to the string pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_string_destroy(char** str);

/**
 * @brief Destroy a byte buffer (uint8_t*).
 *
 * Frees the memory allocated for the byte buffer.
 *
 * @param buffer Pointer to the buffer pointer. Will be set to NULL after freeing.
 */
CMAVSDK_EXPORT void mavsdk_gripper_byte_buffer_destroy(uint8_t** buffer);

// ===== Callback Typedefs =====
typedef void (*mavsdk_gripper_grab_callback_t)(const mavsdk_gripper_result_t result, void* user_data);
typedef void (*mavsdk_gripper_release_callback_t)(const mavsdk_gripper_result_t result, void* user_data);

// ===== Gripper Creation/Destruction =====
CMAVSDK_EXPORT mavsdk_gripper_t mavsdk_gripper_create(mavsdk_system_t system);
CMAVSDK_EXPORT void mavsdk_gripper_destroy(mavsdk_gripper_t gripper);

// ===== Methods =====

/**
 * @brief Gripper grab cargo.
 *
 * @param gripper The gripper instance.
* @param instance 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_gripper_grab_async(
    mavsdk_gripper_t gripper,
    uint32_t instance,
    mavsdk_gripper_grab_callback_t callback,
    void* user_data);


/**
 * @brief Get the current grab (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param grab_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_gripper_result_t
mavsdk_gripper_grab(
    mavsdk_gripper_t gripper,
    uint32_t instance);


/**
 * @brief Gripper release cargo.
 *
 * @param gripper The gripper instance.
* @param instance 
 * @param callback Function to call when new data is available.
 * @param user_data User data to pass to the callback.
 */
CMAVSDK_EXPORT void mavsdk_gripper_release_async(
    mavsdk_gripper_t gripper,
    uint32_t instance,
    mavsdk_gripper_release_callback_t callback,
    void* user_data);


/**
 * @brief Get the current release (blocking).
 *
 * This function blocks until a value is available.
 *
 * @param telemetry The telemetry instance.
 * @param release_out Pointer to store the result.
 */
CMAVSDK_EXPORT
mavsdk_gripper_result_t
mavsdk_gripper_release(
    mavsdk_gripper_t gripper,
    uint32_t instance);


#ifdef __cplusplus
}
#endif

#endif // CMAVSDK_GRIPPER_H